# Changelog

All notable changes to nen. Versions are git tags on `main`; a tag is not a release — see [Install](README.md#install).

## Unreleased

### Added

- **pr** — `nen pr request-reviews` gains `--add-bots <id,...>`: GitHub Bot node ids, routed through the `requestReviews` GraphQL mutation's `botIds` (`union:true`) — the one mutation that actually resolves a Bot reviewer, unlike `gh pr edit --add-reviewer`'s `requestReviewsByLogin`, which never resolves a Bot at all (verified live against a real Copilot reviewer node). Every `--add-reviewers` login is now resolved FIRST — against the pull request's own known bots (its `reviewRequests`/`timelineItems`) and `--target`'s collaborators — and routed to whichever mutation actually reaches it; a login resolving to NEITHER is refused at exit **2**, naming it, and pointing at `--add-bots`. `--dry-run` performs the same resolution and prints which route each name or id would take, without requesting anything. The bot route reports success from the mutation's OWN response (which bots now read as pending review), never assumed from the ids this verb sent — the identical mutation call has been observed answering `NOT_FOUND` for a botId under one token and succeeding under another, a permission-scoped difference in what a token can resolve, not a flake (closes #160, [#174](https://github.com/zheref/nen/pull/174))
- **schema**, **shu** — a launch target's device may say which of the probe's own states count as **ready**: `project.launch.<name>.device.readyWhen`, `{ "field": <n>, "in": [...] }` for a probe that prints lines or `{ "path": "<key>", "in": [...] }` for one that prints JSON. **A device that is PRESENT is not a device that is READY**, and until now nen read the first and reported it as the second: the row that carries a device's name also carries its state — attached is not paired, paired is not unlocked, present is not finished booting — so a launch matched the name, resolved an id off an unusable row, printed the probe green, and every command after it failed one at a time against a device that was never going to answer. `field` counts whitespace-separated tokens on the device's own row with the row's **first token as field 1** — the way a reader counts columns on their own screen, which is where a declaration is written from — and `path` is a dotted key read off the object whose `name` matched, or off an enclosing object up to two levels out, the same walk the id already makes (so the cousin shape, name in one sub-object and state in its sibling, is readable without a second rule). Values are compared as whole strings, verbatim, for `device.name`'s reason; a JSON `true` or `3` at the named path is compared as `"true"` and `"3"`, so a boolean readiness flag needs no second shape. Validated **at load, by pointer**: exactly one of `field`/`path` (a rule naming both would have nen choose against a document it has not seen; naming neither says where to look nowhere at all), `in` non-empty and every entry a non-empty string, `field` a whole number ≥ 1 — a zero-indexed rule would read one column to the left of the state on every launch and refuse forever while the file looked right — and the whole rule refused on a device with **no `resolve` probe**, where nothing is spawned and it could never be read, exactly as `project.launch.<name>.artifact` is refused when no after-step names `{artifact}`. A state is read only where the rows carrying the name **agree** about it — one device described twice is one device, but two rows naming two states is two answers and nen reports neither, exactly as it picks neither of two competing ids. A row present but not ready is **exit 5 naming the device, the state seen and the states accepted**, listing what the probe offered — the same discipline the absence refusal already followed — and it answers **before** the missing-id refusal, because a device whose state is the reason it is unusable routinely prints a row with no id on it and "the probe gave nen no id" is then the true sentence that helps least. `--dry-run` prints the rule as a `readiness:` line under the device (it is the declaration's rule rather than a reading, so it is knowable with nothing connected), and `--json`'s `target.device` gains `readyWhen`, `null` on every device that declares none. Absent, behaviour is unchanged in every particular ([#176](https://github.com/zheref/nen/pull/176), closes #165)

### Fixed

- **pr** — `nen pr request-reviews --add-reviewers` accepts a **team slug** (`org/team`) again. [#174](https://github.com/zheref/nen/pull/174) started resolving every `--add-reviewers` login as a Bot-or-collaborator, uniformly, before choosing a route — which also caught a team slug, refusing it at exit **2**, even though `gh pr edit --add-reviewer` has always accepted one, unresolved, through the same `requestReviewsByLogin` mutation a User login travels; neither of that resolution's two reads can even answer for a team (a Bot's `login` never carries a `/`, and `collaborators(login:)` only ever resolves a user). An entry containing a `/` is now recognised as a team slug and routed straight to `gh pr edit --add-reviewer`, no bot-or-collaborator lookup at all — reported as `team` under `--dry-run` and `--json`'s `routing[]`; a bare login is unaffected and still refuses at exit 2 when it resolves to neither ([#177](https://github.com/zheref/nen/pull/177))
- **pr** — `nen pr request-reviews`'s empty-input refusal now names its OWN flags: `no reviewers named -- --add-reviewers takes a comma-separated list of logins, or --add-bots a comma-separated list of node ids`, rather than `--reviewers`, which belongs to `pr ready`/`pr next-blocker` and was never this verb's own spelling (closes #95, [#174](https://github.com/zheref/nen/pull/174))
- **shu** (security, `CWE-59`) — every declared path a run reads or writes is contained against the repository's **real** root, symlinks resolved, at the moment of use. `insideRepo` (`src/shu/run.ts`) asked only the lexical question, so a `build/payload` that reads as plainly inside the tree walked straight out of it whenever `build/` was a symlink: `project.lanes.<lane>.cwd`, a `path` precondition, a `project.verbs.<lane>.<verb>.artifacts[i]`, a `project.launch.<name>.artifact`, a step's `stdoutTo`, and the reports `shu coverage` and `shu test-report` read back — all of them, and `--dry-run` reported `ok` about every one. It asks `src/repo/contain.ts`'s `realContainment` too now and refuses at exit **2**, naming the link and where it points, before anything is spawned, read or written (`nen report render --out` and `nen scaffold init` already did, through that same module; `refuseUnwritableRedirect`'s private copy of the test is gone, so the rule is asked in one place rather than three). A symlink that stays inside the tree is ordinary and still allowed — the question is where the path lands, never whether a link was involved. No new attacker capability: an after-step's literal argv is not containment-checked at all, so a declaration that can write that symlink can already name an absolute path outright. What was lost is the guarantee the refusal *states*, and the assurance a reviewer takes from a clean `--dry-run` — a check that reports clean about a property it never tested is the failure mode nen refuses everywhere else. `nen schema check` is deliberately unchanged: the loader has no filesystem, so pointers are checked at load and paths at use, and `docs/USAGE.md` gains one "Containment" section under Conventions stating the rule once (closes [#157](https://github.com/zheref/nen/issues/157))
- **shu** — `nen shu warmup` no longer fails half-way through a run when the trunk is checked out in **another worktree** of the same repository — the ordinary shape of a worktree-based flow (a primary checkout standing on `main`, every effort in its own `git worktree` beside it), where `git branch --force main origin/main` is refused outright: *fatal: cannot force update the branch 'main' used by worktree at '…'*. Before the fast-forward the verb now reads `git worktree list --porcelain` (step 4a, with the other checks that need no mutation, so a list that does not answer refuses at exit 2 **before** the fetch rather than after it), matching the **full** ref so a branch called `feat/main` is never mistaken for the trunk. When another worktree holds it the local update is **skipped** — the report says `trunk held by worktree <path>; cutting from origin/<trunk> directly` — and `--branch` is cut from `origin/<trunk>` exactly as it always was: the cut never read the local ref, which is why the local fast-forward was never needed for it. That fast-forward now has three shapes rather than two — a merge when *this* checkout is on the trunk, a ref move when no worktree holds it, and nothing at all when another one does. `--dry-run` predicts the same decision, where it used to print `git branch --force main origin/main` and exit 0 while the real run failed on that exact line: its guarantee changes from *runs nothing* to **mutates nothing**, plus exactly ONE closed-list read-only command (`git worktree list --porcelain`). That row carries its real exit code and is labelled `ran:` while every planned row is labelled `would run:`, and `--json` gains a top-level **`dryRun`** boolean (after `discard`) because `steps[].exitCode` can no longer tell the two forms apart on its own. The two forms tell their own worktree from another's differently and reach the same answer: a real run compares the branch it is standing on (git allows a branch in exactly one worktree), a dry run compares `--repo` against git's own paths, resolving both through `realpath` so a checkout under macOS's `/var/folders` symlink is never reported as held elsewhere by itself. A diverged trunk still refuses, and that refusal now says this run would not have moved it (closes [#168](https://github.com/zheref/nen/issues/168), [#173](https://github.com/zheref/nen/pull/173))
- **wc** — `nen wc classify` **classifies** a detached `HEAD` instead of refusing it. It used to throw at exit **1** — the code this family reserves for *the tree is not clean* — and print that prose on **stdout** under `--json`, so a caller could tell neither "not clean" from "cannot classify" nor parse the answer at all; a headless session running a warm-up in a worktree added with `--detach` hit it first, and a bisect or a rebase step reaches it the same way. The classification is decided by *trunk-or-not* and *dirty-or-not*, and a detached `HEAD` answers both, so the branch is one **field of the answer** rather than a precondition of it: `state.branch` is now `string | null` and a new `state.detachedAt` carries the short sha (`git rev-parse --short HEAD`, asked only when `git symbolic-ref` fails), `isTrunk` is false there whatever `--base` says (`must-move` cannot apply — a commit made on a detached `HEAD` lands on no branch), and every evidence line reads `on a detached HEAD at <sha>` where it read `on '<branch>'`. Text output gains a `branch:` line on **every** path — a name, or `(detached HEAD at <short sha>)` — so nothing has to be inferred from an absence. Exit codes still follow the classification: 0 for all three cases. The fail-closed discipline is untouched and one refusal remains, now saying what it means: a `HEAD` that names no branch **and** resolves to no commit (a repository with no commits yet) has no working copy to classify (closes [#163](https://github.com/zheref/nen/issues/163), [#173](https://github.com/zheref/nen/pull/173))
- **parse** — `nen parse <skill> --grammar "at [<gate:…>]" --line ""` is accepted instead of refused. An empty line used to be exit 2 (*"the line must open with the literal 'at'"*) while `--line "at"` was exit 0 with the clause absent — so the ORDINARY invocation of a skill whose only clause is optional was the one spelling a caller had to be told about, and the refusal read like a malformed invocation. When **every slot** a grammar declares is bracketed, the empty line now parses identically to the bare literal, whichever side of the brackets the template writes the separator on (`[onto <slot>]` already worked; `at [<gate>]` did not). A grammar carrying a **required** slot is untouched: an empty line still refuses at exit 2, naming that slot, with the corrected line to paste. The echo gains a line for an optional slot nobody filled — `<name>: (clause absent)` — because a slot simply MISSING from the echo cannot be told apart from a template that never declared it, which is how a successful parse of such a grammar used to print nothing at all; `slots[]` under `--json` is unchanged and still carries only what the line supplied. `nen parse --help` and [`docs/USAGE.md`](docs/USAGE.md#nen-parse-skill) document both spellings (closes [#170](https://github.com/zheref/nen/issues/170), [#173](https://github.com/zheref/nen/pull/173))
- **shu** — with `--target` given, `dev`/`run` check the target's **declared verb before the lane's seat**, so a target declared for the other long-running verb is nen's own exit-2 refusal naming the fix (`run 'dev --target <name>'`) rather than the lane's exit 4. A target belongs to one verb — a fact about the declaration, true on every lane and every host — while a seat is a fact about one row, and rendered first the seat won on any lane where the other verb is `unsupported` or simply undeclared: `nen shu run --target <a dev target>` answered `'run' is unsupported on lane '<lane>'`, true, and a dead end that sent the maintainer to write a `run` row they never wanted, while nen already held the sentence that ends the problem one check further down. The rest of the order is unchanged and pinned: a lane's seat still answers **4** when the target's verb *does* match (there the seat is the whole answer and there is nothing better behind it), a target with no command line at all still answers 4 in the repository's own words, and an undeclared `--target` still refuses at 2 on the lane the caller named ([#176](https://github.com/zheref/nen/pull/176), closes #166)
- **shu** — `{artifact}` is substituted into a launch target's after-steps **relative to the directory those steps run in** — the lane's — rather than as the repository-root-relative string the declaration writes. Two roots, one string: `artifacts` and `project.launch.<name>.artifact` are stated against the repository root like every declared path, while an after-step is spawned with its cwd set to `project.lanes.<lane>.cwd`, so on any lane whose cwd is not `.` the installer was handed a path that resolved against the wrong root and answered "no such file" about a file that was sitting there. A lane at the root — which is nearly all of them, and is how this stayed invisible — gets back the identical string and passes exactly the bytes it always did, which is why the fix is relative rather than absolute: an absolute path would also resolve correctly and would change what **every** existing launch target spawns, including a v0.4.0 one. The `substitutes:` line says both strings when they differ (`{artifact} <- ../build/App.app  (declared build/App.app, as the after-steps' own directory sees it -- lane '<name>' does not sit at the repository root)`) while `artifacts:` keeps reporting the repository-relative string: the two answer different questions — what does this build produce, and what will the child receive — and collapsing them would hide the rebasing rather than show it. `--json`'s `target` gains `artifactAs`, what `{artifact}` is actually substituted with, beside the `artifact` that is the declared override. The target's own `artifact` override inherits the rebasing rather than being a second rule, and `{device.id}`, which has no path semantics, is untouched ([#176](https://github.com/zheref/nen/pull/176), closes #159)

### Changed

- **docs**, **schema** — `Hatsu-Agent` and `Akatsuki-Agent` are documented as the two conventional provenance-trailer keys this project's family uses to record which plane (local vs. autonomous CI) made a commit — never an AI-authorship claim, and neither shipped as a default. `docs/USAGE.md` gains a "Two provenance trailers" subsection under [`nen/workflow.json`](docs/USAGE.md#nenworkflowjson), linked from `nen scaffold init`'s `--agent-trailer` notes; `templates/workflow.json`'s `commits` block gains a `$comment` naming both (the written default's `allowedAttributionTrailers` stays empty — nen ships no convention). nen's own `nen/workflow.json` now admits both: `commits.allowedAttributionTrailers` is `["Hatsu-Agent", "Akatsuki-Agent"]`, with a `$comment` stating the maintainer's ruling that `Akatsuki-Agent` is recorded only by an Akatsuki roster (CI-plane) agent and `Hatsu-Agent` only by Hatsu's local roster ([#164](https://github.com/zheref/nen/pull/164))
- **stage** — `nen stage triage` reports a git-ignored path in its own `ignored` bucket instead of `flagged`: a fact ("this cannot be staged without `-f`") is no longer indistinguishable from a question ("does this need a human's yes"), which used to bury the rows that actually need a decision under thousands of `node_modules/`- or surface-mirror-shaped ones on a repository with a large ignored tree. Text output gains an `ignored: <n> file(s), not listed` count (this verb carries no `--verbose` flag, so the paths themselves print only under `--json`, which gains a full `ignored[]` array, each entry `{ path, reasons[] }`); `flagged` now holds only paths a plain `git add` could actually stage, and **the exit code follows `flagged` alone** — a tree that is entirely ignored rows is exit 0 rather than 1. An entry that is both ignored and secret-shaped (a `.env` inside an ignored directory) stays in `ignored` with `secret-shape` recorded alongside `ignored`, and never appears in `flagged` ([#171](https://github.com/zheref/nen/pull/171), closes #169)

## v0.5.0 — 2026-09-10

### Fixed

- **repo** — `nen repo resolve` and `nen repo scenario` refuse a `--repo` (or, for `resolve`'s no-token form, a current directory) carrying NEITHER `nen/repos.json` NOR the legacy `schemas/repos.json` at exit **2**, naming the file, instead of falling through to a generic exit-1 failure indistinguishable from an unresolved token or an unrecorded scenario. A missing registry is a PRECONDITION neither verb can proceed past at all — the same class of thing as an omitted `--repo` or `--from` beside a token, both already exit 2 in this family — not a data problem inside a file that opened fine. A registry that is present but malformed is unaffected and stays exit 1, matching how `nen commit format` already treats a malformed (as opposed to absent) `nen/workflow.json` (`nen repo inventory` never reads the registry and is unaffected) ([#153](https://github.com/zheref/nen/pull/153))
- **shu** — `eslint.config.js` adds `.claude/**` to its ignore list, beside `coverage/**`: `eslint .` was sweeping every sibling worktree under `.claude/worktrees/`, warning on a stale `eslint-disable` comment inside another effort's generated `coverage/lcov-report/*.js` on every `nen shu lint` run. `.gitignore` gains `.claude/worktrees/` (not the whole `.claude/` directory, which carries no tracked files today but may hold committed configuration later) ([#153](https://github.com/zheref/nen/pull/153))
- **shu** — `docs/USAGE.md`'s `nen shu dev` section now says, in the `device.name` matching paragraph, that the match is exact — a string comparison with no Unicode normalisation: a device name macOS writes with a typographic apostrophe (U+2019, as in `Sergio’s iPhone`) must be declared with that same character, never the straight `'` (U+0027) a keyboard types, or the probe's output will not match it ([#153](https://github.com/zheref/nen/pull/153))

### Changed

- **schema** — **breaking**: the `schemas/` fallback v0.3.0 announced and v0.4.0 held open is **removed**. `nen/` is the only directory any taxonomy-reading verb ever reads from; a repository still carrying a file only under `schemas/` is refused exactly like one carrying it nowhere, and the refusal names the migration (`nen scaffold init --accept-detected`, or copy the file by hand). `nen schema check` reports the state differently now that there is no read to protect: a REQUIRED file present only under `schemas/` FAILS by that same refusal, and a file that loaded from `nen/` with a `schemas/` copy still sitting beside it is a `warn` **leftover** (naming the `git rm` that clears it) rather than the old `FAIL`-on-differing-bytes *shadowed leftover* — `nen/` is the only file anything reads now, so a stale duplicate is clutter to delete, not a correctness risk, and whether its bytes still agree no longer changes the verdict. `--json`'s `checks[]` rows drop `location`, `shadow` and `shadowed`, and gain `legacy` (boolean: a `schemas/<file>` copy is present on disk, detected, never read). `nen scaffold init`'s `schemas/` → `nen/` copy step is unchanged — it is the migration path, not the fallback, and stays the way out. One ripple from removing the fallback at its source: `nen repo resolve`/`nen repo scenario`'s exit-2 "no registry" refusal above now also fires for a repository carrying ONLY the legacy `schemas/repos.json`, where it previously resolved through the fallback and never refused at all. ([#156](https://github.com/zheref/nen/pull/156))

### Added

- **shu** — a step may say where its stdout goes: `"stdoutTo": "<repo-relative file>"`, on a `{exe, argv}` invocation or on one entry of its `steps`. There is still no shell and no redirection **operator** — the seam already captures a child's stdout, and this says to write those bytes to a file rather than relay them, which is the whole of what a tool that PRINTS the thing nen then parses has needed (the bundled Apple profile's own coverage row extracts its report to stdout with `xccov view --report --json`, and `nen shu coverage` reads a *file*; that profile's advisory note now says so, and `docs/STACK-MATRIX.md` is regenerated). Refused **at load**, by pointer, for a path that is absolute, carries a `..` segment, or carries a glob character (`*`, `?`, `[…]`) — nen expands nothing, so a glob would create one file with that character literally in its name while every later reader went looking for the expansion; refused **at the run**, before anything spawns and for every step at once, when a directory is already at the path or a symlink would land the write outside the tree (`realContainment`, the same check `nen scaffold init` makes before it writes). Refused outright on `dev` and `run`: those hand this terminal to the child, so nen never sees a byte of their output and the file could not be written however hard it tried. Parent directories are created; the file is written whatever the tool exited — a half-written report is what a redirect leaves and what a reader debugging the failure opens — and is **not** written for a step that could not start. That step's stdout no longer reaches the terminal (printing it as well would bury the report under the document nen was asked to file away); its **stderr** still does. `--dry-run` prints `stdout -> <path>` on the step, `renderReport` lists every such file on a `stdout to:` line beside `artifacts`, `log.why` names them, and `--json` gains `steps[].stdoutTo` (`null` on every step without one, never omitted). It works on a WATCHED step too -- one carrying a `stall` guard, whose output arrives from the streaming seam as chunks rather than as one buffer: the chunks are held and joined rather than relayed, and the file gets the child's own bytes with the one normalisation the captured path already applies (over the joined text, because a `\r\n` can straddle two chunks) ([#152](https://github.com/zheref/nen/pull/152))
- **shu** — new precondition kind `port`: `{"kind": "port", "value": 3000, "expect": "listening" | "free", "why": "…"}`, asserted by opening a TCP connection to **`127.0.0.1:<port>`** through a new `Seams.probePort` member and destroying it — node's `net`, no third tool, nothing read or written either way, and the host is not a parameter, so no declaration can make nen connect anywhere else. `listening` is satisfied when the connection is accepted, `free` when it is **refused** — a refusal is a positive fact and is what "the port is free" looks like on every platform. A connect that neither completes nor is refused within 500 ms is `satisfied: null` — *cannot assert* — and refuses at exit 2 like every other unassertable row, because "nothing answered in time" is not "nothing is there". The value is a JSON **number** in 1–65535 (`"3000"` is refused at load naming the fix, since a value nen has to guess at is a check that could never honestly pass), `expect` is **required** on a port row out of a closed two-member set and **refused on every other kind** rather than silently dropped, and a port stated as a *list* is "cannot assert" exactly as a listed `path` already is. `ASSERTABLE_KINDS` gains `port`, so `nen shu detect`'s own notes about what nen asserts say all three; the precondition table prints `(expect …)` beside the value, and `--json`'s `preconditions[]` gains `expect` — `port 3000 -- FAIL` is meaningless without it. This is the one thing in `nen shu` that is asynchronous: `runVerb` and the three verbs that call it now answer with a promise, while `detect`, `tools` and `evidence` stay synchronous because `nen scaffold init` calls this dispatcher for `shu tools` from inside a synchronous report builder ([#152](https://github.com/zheref/nen/pull/152))
- **schema** — `project.evidence`'s own BLOCK KEY is guarded against a near-miss, exactly as `project.launch`'s already was and in the same words: `evidences`, `Evidence` or `evidenc` would be preserved as an unknown key, read by nobody, and `nen shu evidence` would refuse at exit 2 saying this repository declares no evidence block — about a file that plainly declares one. Both guards now come off one table, each carrying its own sentence about what a silently-preserved misspelling costs. The older optional blocks (`targets`, `hosts`, `toolchain`, `profiles`) are still deliberately unswept at the block level, because a declaration written against 0.3.0 may already park a near-miss key there; `targets` keeps the per-entry guard it has ([#152](https://github.com/zheref/nen/pull/152))
- **pr**, **issue** — `nen pr edit-body --target <owner/name> --pr <n> --body-file <path>` and `nen issue edit-body --target <owner/name> --issue <n> --body-file <path>` replace an object's body OUTRIGHT with a file's bytes — no trimming, no template — through `gh pr edit --body-file` / `gh issue edit --body-file`, retiring the last hand-run `gh … edit --body-file` a mechanised choreography had to drop back to (the body-replacement half of the gap `issue comment` closed for posting). Both certify the number BEFORE any write, and — unlike `issue comment`, which deliberately accepts either object class — each refuses the other family's object at exit 2: `pr edit-body` reads `gh api repos/<target>/pulls/<n>` and refuses a 404/410 (worded so it never claims the number IS an issue, only that it is not a pull request), and `issue edit-body` reuses `readIssue`'s existing `issues/{n}` discriminator and refuses a number carrying a `pull_request` payload. `--dry-run` still performs that certifying read — this pair is not network-free, the same shape `issue attach-sub`/`consolidate-close` already have — then prints the target, the number, the byte count and the first/last line instead of writing. `--json`: `{ contract: "nen.pr.edit-body/v0.1" | "nen.issue.edit-body/v0.1", target, number, bytes, written, dryRun }`. Both are `dry-run-gated` in izanami's automation-policy table ([#151](https://github.com/zheref/nen/pull/151))
- **surface** — new family `nen surface` (`mirror generate`, `mirror check`): one skills directory rendered into the layout and frontmatter another agent surface documents for itself, and the drift check that says whether the committed copy is still an image of it. `nen surface mirror generate --source <dir> --surface codex|cursor --out <dir>` writes `<out>/<name>/SKILL.md` for every `<name>/SKILL.md` under `--source`: the **body verbatim**, the frontmatter reduced to the keys that surface documents, and — with `--invocation-prefix` — every `<prefix><name>` mention respelled the surface's own way. **Every per-surface difference is a row in `src/surface/rules.ts`, never a branch in the generator**, and each row carries the URL every fact in it was read from (read 2026-09-10): `codex` keeps `name`/`description` only, spells an invocation `$<name>`, and — having no markdown persona file — folds each `--agents` persona into a `## <name>` section of a generated `AGENTS.md`; `cursor` keeps the whole documented key table (`name`, `description`, `paths`, `globs`, `disable-model-invocation`, `icon`, `color`, `metadata`), spells it `/<name>`, and writes one subagent file per persona under `agents/`. A surface that documents **no** invocation spelling leaves the mention exactly as written — this verb never invents a syntax nobody published. The `<prefix>` itself is a flag and never a literal in this binary, for the same §3 reason [`canon mirror generate`](https://github.com/zheref/nen/blob/main/docs/USAGE.md#nen-canon-mirror-generate)'s `--header-template` is one. Every generated file carries `<!-- GENERATED by nen surface mirror (surface: <name>) -- do not edit; edit the source and regenerate -->` as its first **markdown** line — *after* the frontmatter fence, not above it, because every one of these surfaces identifies a skill by frontmatter at the start of the file and a banner on line 1 would produce a document the surface silently declines to load. `generate` writes only what changed, deletes an orphan whose source is gone (and the directory it emptied), and **refuses, before writing a byte, any destination that exists and carries no marker** — the case that makes it non-optional is `AGENTS.md`, which people write by hand and which an `--out` pointed one directory too high would otherwise destroy. `nen surface mirror check` regenerates in memory, writes nothing at all, and reports `missing`/`extra`/`stale`/`hand-edited` exactly as `canon mirror check` does, exit 1 on any drift; `stale` is where that verb's `--ref` sits here — the marker names the **surface**, so a mirror generated for one and checked against another was really generated and really is out of date. Refused at exit 2: an `--out` inside `--source` (the mirror would become part of its own source), a `--surface` with no row, a `--source` holding no `SKILL.md` (an empty generation would delete the whole mirror as orphaned), a `SKILL.md` with no frontmatter or missing a key the surface documents as required, and `--dry-run` on `check`. The mirror's filename universe has **two** conditions — a file must sit at one of the row's own locations **and** carry a generated marker — so a `SKILL.md` somebody wrote by hand in the mirror directory is never overwritten, never deleted as an orphan and never reported as `extra`, exactly like a `references/` file beside a generated skill; `check` and `generate` read that same list, so what one reports the other clears. On a surface that keeps personas as files, a persona that would mirror to an EMPTY frontmatter block (no fence in the source, or a fence holding only keys that surface does not read) is refused at exit 2 rather than written as a persona file with nothing for the surface to route on — on a surface whose personas are prose, the same file is fine ([#149](https://github.com/zheref/nen/pull/149))
- **wc** — new verb `nen wc squash --onto <ref> --message-file <file>`: folds every commit `git merge-base <onto> HEAD` finds into ONE, whose message is validated to the same shape `nen commit format` enforces (a Conventional Commits header ≤ 72 characters, trailers as `Key: value` lines in the final paragraph, and any attribution trailer this repository's `nen/workflow.json` does not admit refused — the same wording `nen commit format` already uses, now shared through `../schema/workflow.ts`'s `attributionRefusalMessages` rather than kept as two copies). Every refusal runs BEFORE a single write, all at exit 2: a dirty working tree (every uncommitted/untracked path named); `--onto` not an ancestor of HEAD (what `git merge-base` found instead, quoted); any commit in the range already reachable from this branch's `@{upstream}` (fetched first, through the seam) — "already published; squashing would rewrite pushed history". Fewer than two commits to fold is **not** a refusal: exit 0, one line, nothing moves. The mechanism is `git reset --soft <merge-base>` then `git commit -F <message-file>`, both through the seam — `reset --soft` only moves the branch ref and the index, so a commit that then fails leaves the original commits recoverable from `ORIG_HEAD` / the reflog, which the verb's own error names. `--dry-run` prints the commits that would fold and the message, spawning neither call. `--json` publishes `nen.wc.squash/v0.1`: `{ contract, onto, mergeBase, folded: [sha, ...], newSha, dryRun }`, `folded` oldest first. Never touches a remote beyond the read-only fetch the upstream check makes, never pushes, never force-anything ([#150](https://github.com/zheref/nen/pull/150))
- **schema** — `nen/workflow.json`'s `notifications` block admits `turn`: `"rung1"` (the default, and what an absent key reads as) or `"all"` — a closed two-value set, refused by pointer on anything else, exactly like every other key this loader validates (`nen schema check`'s FAIL row). It decides how loud an ORDINARY turn is — one with no gate: `"rung1"` rings only the first rung `notifications.rungs` lists, `"all"` rings every rung `rungs` lists, every turn. A gate always rings everything `rungs` lists regardless of `turn`, and `turn` can only **withhold** an escalation `rungs` already grants — it can never conjure a rung `rungs` does not list. nen reads and validates the key and fires none of the three rungs itself, exactly as the rest of `notifications` already works: this is policy data for whichever host hook rings them. `templates/workflow.json` now states it explicitly, `"rung1"`, with a one-line `$comment`, so `nen scaffold init`/`new` write it into every fresh policy file rather than leaving it implicit ([#155](https://github.com/zheref/nen/pull/155))
- **shu** — a launch target may override the two defaults it is the wrong place for: `project.launch.<name>.lane` and `project.launch.<name>.artifact`, both optional and both absent-means-exactly-what-it-meant-before. `lane` names which lane the target's verb, `args` and after-steps are read from; absent it is the lane `--lane` named, or `project.defaultLane` with no flag. It exists because a **device build is routinely a different declared row from the one a developer iterates in** — the same `dev` verb on two lanes is two argvs and two artifact sets — so a target that could not say which lane it belongs to would install whatever the default lane happened to produce. The target's lane is read **before anything is rendered**, so a target whose own lane is the only one declaring the verb is reachable without retyping `--lane` — the flag the key exists to make unnecessary — and everything downstream is that lane's: the cwd, the preconditions, the host verdict and the artifacts; the lane must be one `project.lanes` declares (refused **at load**, by pointer, listing the ones that are, rather than at the moment somebody reaches for their phone); and an explicit `--lane` that contradicts it is exit 2 naming both, because two stated facts are not something nen picks between. `artifact` is the repo-relative path `{artifact}` is substituted with **instead of** the verb's first `artifacts` entry: "the first artifact" is the right answer for the thing a lane *builds* and the wrong one for the thing a device *installs*, and a build routinely produces both, in that order. It is refused **outside the tree** (`project.launch.<name>.artifact`, the same containment rule every declared path gets, checked before the dry-run report is printed so a plan naming an escaped path is never shown as fine), refused as an empty string (which would substitute into an after-step as nothing at all), and refused when **no after-step names `{artifact}`** — the key has exactly one effect, so a target that never writes the token has stated a path nothing reads. With an override present, `{artifact}` on a verb declaring no artifacts is no longer a refusal: the target supplied what the token needed. `--dry-run` says which of the two the substitution reads (`{artifact} <- <path>  (project.launch.<name>.artifact, not the verb's own)`) and the `target:` line says whose decision the lane was, while `artifacts:` keeps reporting what the **verb** declares — the two answer different questions and collapsing them would hide the override rather than show it. `--json`'s `target` gains `lane` and `artifact`, `null` on a target that declares neither. Both keys join the near-miss set, so `lanes` and `artifacts` on a launch target are refused by pointer instead of preserved into a silent default. `--dry-run` keeps **both** tokens unfilled in the printed step — `{artifact}` included, even though nothing has to be spawned to know it — because the printed argv is what a real run composes and `substitutes:` is where a value nen has read is stated. And one rule the pair made worth stating: `{device.id}` or `{artifact}` written into `project.launch.<name>.args` is now exit 2 naming the token, because substitution reaches the target's `after` steps and nowhere else — `args` is appended to the verb's own argv, so a token there was not unfillable but simply unfilled, and reached the child process as itself; [`nen shu dev`](docs/USAGE.md#nen-shu-dev) carries the worked transcript ([#158](https://github.com/zheref/nen/pull/158))
- **shu** — a captured verb may declare a **stall guard**, and nen runs the repository's own remedy when a step stops making progress: `"stall": { "elapsedMs": <n>, "quietMs": <n>, "onStall": { "exe", "argv" }, "maxStrikes": <n, default 2> }` on an invocation (it reaches every step declaring none) or on one `steps[]` entry (which wins). **Nen never knows what to kill and now does not have to**: some toolchains hang — a compiler process wedges, the build stops emitting and never finishes, and the fix is to kill the wedged *grandchild* and let the build respawn it — and which process that is, is exactly the toolchain knowledge `src/shu/run.ts` and `src/shu/render.ts` may not carry. The repository declares the remedy as ordinary argv (never a string: there is no shell) and nen contributes the two numbers that decide **when**. It fires once **both** budgets are past — total elapsed **and** a quiet window with no output — because a guard acting on silence alone would fire at a healthy build that legitimately went quiet early on; each firing restarts the quiet window and costs a strike, and after `maxStrikes` the step is reported **stalled** at exit 1. **Nen never signals the child it started**, at any strike count: on a stall it stops watching, stops waiting, releases its own hold on the process and says plainly that it is still running and is the caller's to stop — the alternative, waiting, is the thing the caller came to be rescued from. Declarable only where nen READS the output (`build`, `test`, `ui-test`, `lint`, `archive`, `coverage`, `test-report`); on `dev`/`run` — which hand the terminal to the child — and on `release`/`deploy` it is **exit 2 naming the set**, never accepted and quietly not honoured. Both budgets and `maxStrikes` are positive integers validated **at load, by pointer** (a default for either budget would be nen deciding what "too long" means for somebody else's build), `onStall` is parsed by the same reader every declared step goes through, and a key one spelling away from one of the four is refused rather than preserved into silence. `--dry-run` prints an `on stall:` line under the step it guards; `--json`'s `steps[].stall` carries the budgets **and** what happened (`strikes`, `at` in milliseconds into the step, `stalled`) — spending every strike and finishing green is the guard *working*, which a count alone cannot say. A stalled step reports `exitCode: null`, because the child never gave one. A step with no guard goes through the captured seam exactly as before ([#154](https://github.com/zheref/nen/pull/154))
- **seam** — `Seams` gains a third runner, `runStreamed`, for a child that must be WATCHED while it runs: output relayed as it arrives with the millisecond each chunk landed on, and an optional watcher consulted on an interval with `{ elapsedMs, quietMs }`. `run` hands back one buffer when the child is already over and `runInteractive` gives the output up entirely, so neither can answer "has this said anything in the last minute" — the question a stall guard is arithmetic over. The watcher's verdicts are `watch`, `reset` (restart the quiet window *after* whatever the watcher just did), `stop` (stop watching, keep waiting) and `abandon` (stop waiting too, leaving the child untouched); **nothing in this seam ever signals a child**. The window arithmetic is one shared function, so `ScriptedSeams` — which gains a `runStreamed` replaying a timeline a fixture states, `{ atMs, stream?, text? }` per event — proves a guard against the same rule the real runner applies to a real clock, with no test sleeping for a three-minute budget ([#154](https://github.com/zheref/nen/pull/154))
- **shu/commit** — a green `nen shu build` records **the tree it proved**, and new verb `nen commit check --repo <path> --require-proof <lane>` reads it back. The build writes `.nen/proof/<lane>.json` (`nen.shu.proof/v0.1`: `contract`, `lane`, `verb`, `treeHash`, `at`, `exitCode`) when every step exits 0, and **removes** an existing one when the build comes out red — a stale proof is a green answer to a question that has since been answered red, and it must never outlive the tree it proved. A dry run writes nothing and removes nothing: it ran no build, so it learned nothing about this tree in either direction. `treeHash` is git's own tree object for the **working copy**, not the index — the build ran with nothing staged and the check runs a moment before a commit with everything staged, so an index-based hash would disagree every time and the proof would be useless in the workflow it exists for. Both sides compute it identically through the seam with a **scratch index** (`GIT_INDEX_FILE` under `.nen/`, removed afterwards): `git add -A`, `git rm --cached` for `.nen/` (a `:(exclude)` pathspec exits 1 in every repository that already ignores `.nen/`), then `git write-tree`. The repository's own index is never read or written, no ref moves, nothing tracked changes — which is what lets `nen parse izanami` classify `commit check` **read-only**. The check exits 0 when the proof is there, is that lane's, and its tree is this tree; exit 1 naming which of the three differences it is (no proof — also what a red build since the last green one looks like; a different lane; the tree has moved, with both hashes); exit 2 for a missing flag, a lane that escapes the tree, or a proof present and unreadable, which is never folded into "absent". A proof is held to its VALUES and not only its field types -- another `contract` may mean something else by the same fields, and `verb`/`exitCode` are the file's own assertion that a green build produced it, so an edited line cannot turn a red build into a green verdict. It **reports and blocks nothing**: no commit is refused and no file is written. It is what makes "never commit over a red build" affordable — the alternative is rebuilding before every commit, or believing a sentence in a transcript. `nen report data` already carried `.nen/proof/<lane>.json` verbatim and is unchanged; it now imports the path from the module that writes it, so where a proof lives is one fact ([#154](https://github.com/zheref/nen/pull/154))

### Breaking / consumer notes

This is the first release since v0.1.0 that REMOVES something a consumer could be relying on, so the removal is the item to read first; the repin rule follows it, and then one sentence per addition.

- The four taxonomy files (`labels.json`, `repos.json`, `colors.yml`, `gates.json`) resolve from `nen/` only, as of this release; an un-migrated repository now fails `nen schema check` and every verb needing a file it finds only under `schemas/`, where v0.4.0 passed it with a `warn`. Run `nen scaffold init --accept-detected` (or copy the four files by hand) before upgrading past this release. `nen schema check --json`'s row shape changed (see above) — a consumer parsing it must update.
- zheref/akatsuki-ai#45 pins three guard constants that still name `schemas/`; that repository needs to repin them to `nen/` in the same change that upgrades its nen pin past this release.
- **Repin.** nen is still on a 0.x line, so a minor bump is the vehicle for a breaking change, not just features — the removal above is one. The contract's zero-major rule reads a `minimum` of `0.4` as `>=0.4.0 <0.5.0` *exactly*, out of range in both directions, so a `nen/contract.json` still pinning `"0.4"` does not admit a v0.5.0 binary at all: `nen shu tools` reports the row `present-but-wrong-version` at exit 5. Reading any of the additions below means an explicit repin — `minimum: "0.5"` plus a `pinned_ref` of `v0.5.0`, in the consuming repository's own PR. Neither moves on its own.
- **shu** — a step may declare `"stdoutTo": "<repo-relative file>"`, writing that step's stdout to a file instead of relaying it to the terminal (its stderr still arrives); refused at load for an absolute, `..`-carrying or glob-carrying path, and refused outright on `dev` and `run`. `--json` gains `steps[].stdoutTo` on every step, `null` where absent and never omitted, so a consumer parsing that document sees a new key ([#152](https://github.com/zheref/nen/pull/152))
- **shu** — new precondition kind `port`, with a **required** `expect` of `listening` or `free`, asserted by connecting to `127.0.0.1:<port>` through a new `Seams.probePort` member. `--json`'s `preconditions[]` gains `expect`, and `expect` on any other kind is now refused rather than dropped. This is the one asynchronous thing in `nen shu`: `runVerb` and the three verbs that call it answer with a promise, so an embedder holding this module must await them ([#152](https://github.com/zheref/nen/pull/152))
- **schema** — `project.evidence`'s own block key is guarded against a near-miss, exactly as `project.launch`'s already was: a declaration spelling it `evidences`, `Evidence` or `evidenc` was preserved and read by nobody through v0.4.0, and is refused by pointer from this release ([#152](https://github.com/zheref/nen/pull/152))
- **pr**, **issue** — new verbs `nen pr edit-body` and `nen issue edit-body` replace an object's body OUTRIGHT from a file's bytes, each certifying the number before any write and refusing the other family's object at exit 2. Both are `dry-run-gated` in izanami's automation-policy table, and both perform their certifying read even under `--dry-run` ([#151](https://github.com/zheref/nen/pull/151))
- **surface** — new family `nen surface` (`mirror generate`, `mirror check`) renders one skills directory into the layout and frontmatter another agent surface documents for itself, and reports drift against the committed copy at exit 1. Every per-surface difference is a data row in `src/surface/rules.ts`, so a new surface is a row and never a branch; a v0.4.0 binary carries neither verb ([#149](https://github.com/zheref/nen/pull/149))
- **wc** — new verb `nen wc squash --onto <ref> --message-file <file>` folds a branch's commits into one whose message is held to the shape `nen commit format` enforces, refusing a dirty tree, a non-ancestor `--onto` and any already-published commit before a single write. It never pushes and never force-anything; the shared `attributionRefusalMessages` now lives in `../schema/workflow.ts`, which is where an embedder importing that wording should read it from ([#150](https://github.com/zheref/nen/pull/150))
- **schema** — `nen/workflow.json`'s `notifications` block admits `turn`: `"rung1"` (the default, and what an absent key reads as) or `"all"`, refused by pointer on anything else. `nen scaffold init`/`new` now write it into every fresh policy file, so a policy file scaffolded by a v0.5.0 binary is refused by a v0.4.0 one as an unknown key — the repin is in both directions ([#155](https://github.com/zheref/nen/pull/155))
- **shu** — a launch target may declare `project.launch.<name>.lane` and `project.launch.<name>.artifact`; both are optional and absent means exactly what it meant before. One thing that was accepted and is not: `{device.id}` or `{artifact}` written into `project.launch.<name>.args` is now exit 2 naming the token, where through v0.4.0 it was appended to the verb's argv unfilled and reached the child process as itself ([#158](https://github.com/zheref/nen/pull/158))
- **shu** — a captured verb may declare a `stall` guard, and nen runs the repository's own declared remedy when a step stops making progress. Declaring one on `dev`, `run`, `release` or `deploy` is exit 2 naming the set, never accepted and quietly not honoured, and a stalled step reports `exitCode: null` because the child never gave one ([#154](https://github.com/zheref/nen/pull/154))
- **seam** — `Seams` gains a third runner, `runStreamed`, and `ScriptedSeams` a fixture-driven counterpart; an embedder that implements `Seams` itself must supply it. Nothing in this seam ever signals a child ([#154](https://github.com/zheref/nen/pull/154))
- **shu** — `nen shu build` now WRITES `.nen/proof/<lane>.json` on a green build and removes it on a red one, and asks git for a tree hash to do it (a scratch index; the repository's own is untouched). A repository that does not ignore `.nen/` will see the file in `git status`; `nen scaffold init` has always added the entry. Every executing verb's `--json` document gains a trailing `proof` key and every `steps[]` row a trailing `stall` key, both `null` where they do not apply
- **commit** — new verb `nen commit check --repo <path> --require-proof <lane>`, read-only, which reports whether this working copy is the tree a green build proved. `nen commit` now refuses a flag the named subcommand does not read, as `nen shu` and `nen issue` already do
- **repo** — `nen repo resolve` and `nen repo scenario` now exit **2**, naming the file, for a target carrying NEITHER `nen/repos.json` NOR the legacy `schemas/repos.json`, where through v0.4.0 they failed at exit 1 indistinguishably from an unresolved token or an unrecorded scenario. A caller branching on exit 1 for "not found" must add exit 2 for "no registry"; a registry that is present but malformed is unaffected and stays exit 1 ([#153](https://github.com/zheref/nen/pull/153))

## v0.4.0 — 2026-09-10

### Added

- **pr** — `nen pr cascade-main` gains `--no-push`: fetch and merge exactly as it always has, then stop — never push, with the log saying `not pushed (--no-push)` and `--json` gaining `noPush: true` (`false` on every other call, never omitted). A conflicted merge's `--json` also gains `conflicts[]`, one entry per unmerged path from `git diff --name-only --diff-filter=U`: `kind` (`both-modified`, `add-add`, `modify-delete`, `delete-modify`) read off `git ls-files -u`'s stage table, and `ours[]`/`theirs[]` — the commits each side contributed to that path since the merge base (`git log --format=%H <mergeBase>..<ref> -- <path>`). Text output lists the same, one block per file. Neither flag changes the existing rule: this verb never runs `git merge --abort`, never picks a side, and never pushes over a conflict. `--no-push` still merges locally — a working-tree/index mutation — so izanami's automation-policy table keeps `cascade-main` `mutating` in every spelling, `--no-push` included ([#141](https://github.com/zheref/nen/pull/141))
- **repo** — nen declares itself: `nen/contract.json` (a `project` block only -- one lane, `nen`, stack `bun-cli`, since none of this release's seven reference stacks names a Bun-native CLI and the field is a free string everywhere it is read; `build`/`test`/`lint`/`dev`/`archive` running this repository's own `bun run` scripts, `coverage` adding `@vitest/coverage-v8` and an `lcov` reporter so `nen shu coverage` has a report to parse, and `ui-test`/`run`/`release`/`deploy` as declared `{"unsupported": "<why>"}` seats) and `nen/workflow.json` (the Hatsu policy block -- branch template, iteration checks, the coverage ladder, notification rungs, the allowed/forbidden commit trailers, the model matrix). `workflow.json` was read by nobody when this landed; the schema, the loader and `nen schema check`'s row for it arrive later in the same release, under the **schema** bullet below. `coverage/` and `Reports/` are added to `.gitignore` and to `eslint.config.js`'s ignore list, so a local `nen shu coverage` run leaves no residue for `nen shu lint` to trip over ([#140](https://github.com/zheref/nen/pull/140))
- **report** — new family `nen report` (`data`, `render`): the facts an effort's report is made of, and the fill that turns them into one. `nen report data --repo <path> --base <ref>` assembles ONE document (`nen.report.data/v0.1`) describing this branch against that base — the commits (`<base>..HEAD`, split on a unit separator so a subject carrying a tab stays one field), the changed files (`<base>...HEAD`, the merge-base set a pull request shows, a rename reported at its **destination** with git's own status token), the `evidence` rows, the lane's coverage report **if one is already on disk**, `.nen/proof/<lane>.json` and `.nen/last-stop.json` verbatim. It **reads and never writes**: four git reads and some file opens, no write path in any flag combination, which is what lets izanami classify it read-only. Every absence is `null` with the reason on stderr — no declaration, no lane, no artifact nen recognises, a report not written yet — because a report verb answering "there is none to show" six different ways is still answering; what is **not** folded into a null is a git command that FAILS, so an unresolvable `--base` is exit 2 naming the ref with nothing read, and a failed `git log`/`git diff` is refused rather than reported as a branch with nothing on it. `--tiers <file>` labels each changed file from a caller-supplied `{ "<tier>": ["<prefix or glob>"] }` table whose **key order is the precedence**; a pattern with no `*`/`?` matches as a path prefix **on segment boundaries** (`src/report` never claims `src/reporting.ts`) and one with them as a narrow glob. `coverage` is parsed by `nen shu coverage`'s own reader (`src/shu/coverage/parse.ts`), so the two verbs cannot disagree about what an lcov file says — the difference is that this one spawns nothing and reads what is there. `evidence` is an empty list in this release **and the empty list is the seam**: those rows belong to `nen shu evidence`, which owns `project.evidence`, and the field ships now so a template written against this contract does not change shape when that verb lands. `repo` carries the checkout's directory NAME and never its absolute path — this document gets filled into a report that gets pasted into a pull request. `nen report render --template <file> --data <file> --out <file>` fills a template with that document: the WHOLE language is `{{token}}` (HTML-escaped), `{{{token}}}` (raw), `{{#each <list>}}…{{/each}}` (nested, with `{{.}}` and `{{@index}}`) and `{{#if <key>}}…{{/if}}` — no helpers, no partials, no comments, no expressions, because a program in a data file is a thing nobody reviews. A token the data has not got is exit 2 **naming it**, since a blank cell in a published report reads as a fact; a present `null` renders empty (the document's own "nothing to say") and an object or list reaching a value tag is refused rather than printed as `[object Object]`. `--out` must resolve inside `--repo` **with symlinks resolved**, and `--dry-run` parses, renders and throws the result away — so its refusals are the real run's — printing every token the template names, and listing them on a refusal too so the advice to run it is not a loop ([#142](https://github.com/zheref/nen/pull/142))
- **shu** — `nen shu test-report` runs the lane's declared **`test`** command and parses the results it produced into one shape: a row per test (`name`, `suite`, `status`, `durationMs`) and the four counts, under `nen.shu.test-report/v0.1`. It declares nothing of its own — it runs `project.verbs.<lane>.test` and reads *that* row's `artifacts` — and reads three formats, chosen by file name and confirmed against the bytes: JUnit XML (one file, **or a whole directory** of them, which is how a runner that writes one file per suite is declared: nen reads every `*.xml` under it, sorted, and merges them), the `testResults[].assertionResults[]` JSON a JavaScript runner writes with its JSON reporter, and the JSON test summary a **declared** result-bundle extraction step writes — nen opens no result bundle itself, because the only supported way to read one is a program no declaration named. The eight outcome words the three formats spell between them collapse to three statuses: an `error` is a **failure**, `pending`/`todo`/`disabled` are **skips**, an `Expected Failure` is a **pass**, and a word nen has not met is a refusal naming it rather than a guess that could make a red suite look green. **A run that failed is still parsed** — the one place this verb disagrees with `nen shu coverage`, because a failing suite is the report anybody asked for — while a run that started *nothing* (a dry run, an unmet precondition, a program that would not spawn) parses nothing, since the file on disk is then certainly an earlier run's. **The failures never move the exit code**, in either direction: it is the run's, and under `--from-artifacts` — which reads the declared results and starts no process at all, the one second read gate in izanami's automation-policy table — it is about the read, so a report full of failures that parsed cleanly exits 0. A damaged report is a refusal naming the file and the field, never a number: a case with no name, a count that is a string, a summary accounting for more tests than it ran, or an `*.xml` inside a directory artifact that is not a test report at all. Suite names that resolve inside the repository are reported relative to its root, so a `--json` document does not carry the path of whoever ran it ([#144](https://github.com/zheref/nen/pull/144))
- **schema** — `nen/workflow.json` is a file nen validates: the delivery loop's **policy**, as data, beside the `nen/contract.json` that says what nen executes. Every key is optional and carries a default, so an ABSENT file is a full policy rather than none — `nen schema check` gains a `nen/workflow.json` row reading `absent (defaults apply)` (deliberately not the contract row's `absent (optional)`: one means *nothing to read*, the other *a policy made of defaults*), naming the ladder, the branch template and the trunk when the file is there, and FAILing by **pointer** when it is there and wrong. `--json`'s `checks[]` carries the row last. The blocks are `branch` (`template`, which must contain `{descriptor}` — a template without it renders one branch name for every effort — and `base`, held to a git branch name a shell reads only once, because it is interpolated into a generated hook), `iteration`, `tests`, `coverage` (whole percentages 0–100 that must **ascend**: nen will not guess which of three rungs was mistyped), `launch`, `reports` (whose `dir` and `captures` are repo-relative with no `..`), `notifications`, `commits`, `monitor`, and an **open** `models` map. Unknown keys are preserved verbatim, and a key one edit from one nen reads is refused by pointer *because* they are — `{"coverage": {"minimun": 90}}` would otherwise be kept, read by nobody, with the ladder silently holding the default the repository meant to change; the near-miss rule `project.targets` already applies, shared rather than copied, and never applied inside `models`. Three fields have no default at all (`models`, `launch.default`, `iteration.lane`), because each would be nen inventing a name rather than a number ([#146](https://github.com/zheref/nen/pull/146))
- **commit** — `nen commit format --repo <path>` refuses, at exit 2, a `--trailer` whose key is attribution-shaped and is not listed in that repository's `commits.allowedAttributionTrailers`, naming the trailer and the file. Attribution-shaped is an enumeration a reader can audit (`Assisted-by`, `Claude-Session`, `Co-Authored-By`, `Generated-by`, `Generated-with`, `Reviewed-by`, `Signed-off-by`) plus every key the file's own `forbiddenTrailers` adds, matched **without regard to case** because every tool that reads the finished commit does. With no workflow file nothing is refused and the verb behaves exactly as it always has; the policy is opened only when the invocation carries a `--trailer`, and a workflow file that is present and malformed is exit **1** naming the pointer — nen will not shape a message under a policy it could not read ([#146](https://github.com/zheref/nen/pull/146))
- **scaffold** — `nen scaffold init` and `nen scaffold new` write a default `nen/workflow.json` into absence, and **generate both git hooks out of it**. The commit-msg hook gains a refusal for every attribution trailer the policy does not admit — resolved once and baked in **as data**, so the hook needs no nen on `PATH` at the moment of commit and cannot drift from the policy between runs — and it fires on **every** commit rather than only an automated one, because a trailer nobody wants in the history is unwanted whoever typed it. A new generated `pre-commit` hook refuses a commit made on `branch.base` (a detached `HEAD` reports no branch and is allowed through, so a rebase or a bisect still finishes), written beside the commit-msg hook in `--hook-path`'s own directory, with the same `unchanged`/`refused`/`--force`-plus-`.bak` ladder — now one implementation for both hooks rather than two copies of one promise. `.gitignore` upkeep appends the policy's `reports.dir` beside `.nen/`, deciding each entry separately so a file carrying one gets the other. A policy that is already there **wins and is never overwritten** (the hooks are generated from it); one that is there and malformed refuses the whole run at exit 2 before the first write. The written default admits exactly the two trailer keys `--agent-trailer` and `--run-trailer` named — nen ships no convention — and every other byte of it is a bundled data file (`templates/workflow.json`), so a reviewer can read what nen would write into somebody's repository without reading a line of TypeScript ([#146](https://github.com/zheref/nen/pull/146))
- **stop** — `nen stop --mark` writes `.nen/last-stop.json` under `--repo` (`{ contract, who, gate, notified, at }`, creating `.nen/` if needed) so a hook on the caller's own host can ring the OS-notification and audible rungs. **Rungs 2–3 stay the host's**: nen only ever shells out to `git` and `gh`, neither is a notification primitive, and this verb still fires nothing — it states the fact and the host acts on it, exactly as it already does for rung 1. The marker is written last, after every refusal, replaces an existing one (the latest stop is the one to ring for), and is the only form of this verb that writes anything, which is why izanami now classifies `stop` **write-flag-gated** on `--mark` rather than plain read-only; `--mark --template` is exit 2, and a marker that cannot be written is exit 1 with the errno ([#146](https://github.com/zheref/nen/pull/146))
- **shu** — `project.launch` and `nen shu dev|run --target <name>`: a local run that lands on a declared **device**. The block is `{ "<name>": { verb, args, device, after, unsupported, why } }`, a project-level map beside `project.targets` and deliberately not folded into it — `targets` says where a build is *sent*, `launch` says which device a local run lands on, and one map would have made `--target production` on `dev` resolve to a destination that sends nothing. The flag is **optional** here and has no default ever, so a bare `nen shu dev` is exactly the line it was before any target existed. Given, the verb becomes three things in order: the declared `device.resolve` probe (spawned through the **captured** seam, because nen has to read it), the lane's own verb interactively with the target's `args` appended, then the target's `after` steps with `{device.id}` and `{artifact}` substituted — `{artifact}` being the first entry of the verb's own `artifacts`. `device.name` is matched **exactly** against the probe's output, as JSON (a `name` property, with `identifier`/`id`/`udid`/`serial` from the same object, one of its direct children, or up to two enclosing objects — the shape a probe that puts the name and the id in *sibling* sub-objects prints) or as plain lines (the line carrying the name, and its first token of six-plus characters that also carries a digit, so a trailing status word is never read as an id); a `kind: "simulator"` device with no probe resolves to its own name and spawns nothing. Nen picks no device: there is no prefix match, no case fold and no "the only one connected", a device the probe did not name is **exit 5 listing what it did offer**, and a name that **two** id-bearing candidates carry is exit 5 naming both — plain output has no field boundaries, so a declared `Handset` is carried by the `Handset Pro` row too, and resolving the first would put the build on somebody else's device at exit 0 (one id reached twice is not an ambiguity). Refused before anything spawns, at exit 2: a `--target` naming no declared key (listing what is declared), a target declared for the other long-running verb, `args` on a multi-step row, `{artifact}` on a verb declaring none, `{device.id}` on a target with no device, and a device that says neither how to find its id nor that it needs none; a target with `unsupported` answers exit 4 in the repository's own words. `--dry-run` prints all three thirds as `would run:` with the tokens **unfilled** plus one `substitutes:` line, and spawns nothing at all — the probe included, which is why [`nen parse izanami`](docs/USAGE.md#nen-parse-izanami) still classifies both verbs `dry-run-gated`. `--json` stays refused on these two without `--dry-run`, and its `target` is then `{ name, verb, args, device: { name, kind, id }, probe, after }`. The loader parses the block rather than preserving it, for `project.targets`' reason one release on: a key one spelling out (`arg`, `devices`, `resolver`, `verbs` — and the block key itself as `launches` or `Launch`, which the one-edit rule alone does not reach) is **refused by pointer naming the key it meant and which misspelling it is** — one letter out, the same word in a different case, or that key with an English plural on it — because preserved it would be read by nobody and two thirds of a launch would silently not happen. The near-miss rule the deploy-target block already had is widened the same way and now says which of the three shapes it matched, instead of calling all of them "one letter away" ([#143](https://github.com/zheref/nen/pull/143))
- **shu** — new verb `nen shu evidence --base <ref>`: matches `git diff --name-status <base>...HEAD` — through the seam, spawning no invocation the target repository declared, only `git diff` itself — against a new project-level `project.evidence` block (`globs`, required and non-empty; `mechanism`, one of `public-mirror` \| `files-changed` \| `embedded`; `scene` and `suiteSuffix`, both defaulted), derives each surviving file's **suite** and **scene** and reports them grouped suite → scene. Suite/scene derivation generalises KroApple's own `ci_scripts/pr_screenshots.sh` (`scene_of()`): the nearest ancestor directory whose name ends with `suiteSuffix` names the suite (suffix stripped), falling back to the immediate parent directory when no such ancestor exists; the scene is the basename with its extension, a trailing `.<n>` and the `test_snapshot_`/`test_` prefixes stripped, in that order. The glob matcher (`*`, `**`, `?`) is a small, dependency-free module of its own, with `**` correctly matching zero directories on either side (`**/*.png` matches a root-level file, `a/**/b` matches `a/b`). A repository with no `project.evidence` block is exit 2 naming it — the one usage refusal this verb has; no changed file matching a glob is exit 0 with an empty `rows`/`suites` set, never an error. `--json` publishes `nen.shu.evidence/v0.1`. `project.evidence` refuses a key one edit from one of its four, by pointer, exactly as `project.targets` already does ([#145](https://github.com/zheref/nen/pull/145))
- **shu** — `nen shu coverage` gains `--touched --base <ref>`: after the run and the parse, nen computes `git diff --name-only <base>...HEAD` in the repository root and narrows `targets` to the rows that diff names — file-grain formats (`istanbul-summary`, `lcov`) matched by exact path, package-grain ones (`cobertura`, `jacoco`) matched when a touched file sits **under** the package (its own path segments appearing in order, with the file itself left over), and `xccov-report` read at **file** grain only under this flag — nen descends `targets[].files[]` instead of stopping at the target row, because "this whole app was touched" is true of nearly every diff. `--touched` requires `--base`; either flag given without the other is exit 2. `--json` gains `touched: { base, files, matched, unmatched }`, and each row gains its own `met` when `--threshold` is also given, on the counts and never the rounded percentage. With `--threshold` absent, nen also loads `nen/workflow.json`'s `coverage.{minimum,recommended,ideal}` through the shared `src/schema/workflow.ts` loader — the same one `nen schema check` validates — and reports each row's own `band` instead: `under-minimum` / `minimum` / `recommended` / `ideal`, exposed as a tenth `ladder` key, `{ minimum, recommended, ideal, source, present }`. An **absent** policy file is the published 80/85/90 default rather than no ladder, so every touched row is still banded and `present: false` says the rungs were assumed rather than declared; a policy that is present and malformed is exit 1 naming the pointer, refused before the declared tool is spawned, since the ladder is read alongside `--threshold`'s number rather than in the middle of the report. The touched-file list is split with `rawLines` and never trimmed, so a path whose own name carries spaces still matches its row ([#147](https://github.com/zheref/nen/pull/147)). Neither ever moves the exit code, in either direction — the same rule `--threshold` itself has always followed (zheref/nen#91 v3 q16)

### Breaking / consumer notes

nen is still on a 0.x line, so a minor bump is the vehicle for a breaking change, not just features. Two of the additions above change what a verb that already existed does: `nen commit format --repo` now refuses an attribution-shaped `--trailer` the repository's policy does not admit, and `nen pr cascade-main --json` carries `noPush` on every call. Nothing was removed, so a `nen/contract.json` pinning `minimum: "0.3"` stays valid for every verb that existed in v0.3.0 — but the contract's zero-major rule reads `0.3` as `>=0.3.0 <0.4.0` *exactly*, out of range in both directions, so that pin does not admit a v0.4.0 binary and `nen shu tools` reports the row `present-but-wrong-version` at exit 5. Reading any of the ten additions below means an explicit repin — `minimum: "0.4"` plus a `pinned_ref` of `v0.4.0`, in the consuming repository's own PR. Neither moves on its own.

- **schema** — `nen/workflow.json` is a file nen validates and `nen schema check` gains a row for it; every key carries a default, so an absent file is a full policy rather than none and an un-scaffolded repository still passes.
- **report** — the new `nen report` family (`data`, `render`) assembles the facts of an effort against a base ref as one document and fills a template from them; neither verb exists anywhere on the v0.3 line.
- **shu** — `nen shu test-report` runs the lane's declared `test` (or reads its `artifacts`) and parses JUnit XML, `.xcresult`, vitest JSON and Gradle results into one row-per-test shape with the four counts.
- **shu** — `nen shu evidence --base <ref>` reads a new project-level `project.evidence` block and groups the evidence artifacts the diff changed into suite/scene rows.
- **shu** — a new project-level `project.launch` block plus `nen shu dev|run --target <name>` land a local run on a declared device; a v0.3.0 binary preserves `launch` as an unknown key but carries no `--target` that could read it.
- **shu** — `nen shu coverage` gains `--touched --base <ref>`, which narrows the rows to the files the diff names and bands each one against `nen/workflow.json`'s ladder when no `--threshold` is given.
- **pr** — `nen pr cascade-main` gains `--no-push`, and a conflicted merge's `--json` gains `conflicts[]`; `--json` now carries `noPush` on every call of the verb, never omitted.
- **commit** — `nen commit format --repo <path>` now refuses, at exit 2, an attribution-shaped `--trailer` the repository's `commits.allowedAttributionTrailers` does not admit — a refusal no v0.3.0 binary makes, so a repository that relies on it must pin 0.4.
- **scaffold** — `nen scaffold init` and `nen scaffold new` write a default `nen/workflow.json` into absence and generate both git hooks out of it: a `pre-commit` that refuses a commit on the trunk and a `commit-msg` that refuses an unadmitted attribution trailer.
- **stop** — `nen stop --mark` writes `.nen/last-stop.json` under `--repo` so a hook on the caller's own host can ring the notification and audible rungs; nen still fires nothing itself.

- The `schemas/` fallback that v0.3.0 announced for removal **in this release** is still read, and its removal moves to **v0.5.0**: the four taxonomy files still resolve from a repository's legacy `schemas/` directory when `nen/` does not carry them, `nen schema check` still names every legacy read, and un-migrated consumers keep working unchanged through the v0.4 line. Every sentence that named the removal point (README's migration note, `docs/USAGE.md`'s, and the `LEGACY_FALLBACK_REMOVED_IN` the CLI quotes) now says v0.5.0, because a binary that announces its own version as the removal point while still reading the fallback is a binary that lies; the deletion is a change of its own, with its own tests, not a line in a release commit.

## v0.3.0 — 2026-09-08

### Fixed

- **shu** — `nen shu detect` proposes `gradle-android`'s two screenshot rows only where the lane's own build actually applies the plugin whose task they run. `{gw} verifyPaparazziDebug` (as `ui-test`, and as `test` wherever `{unitTestTask}` answered) was written into every `gradle-android` lane whose wrapper `detect` could resolve — the markers confirm a wrapper, a settings file and the Android *application* plugin, and none of the three says anything about Paparazzi — so a lane without it got a declaration whose first run is `Task 'verifyPaparazziDebug' not found in root project`, stored unread by `scaffold init --accept-detected`. Both rows are now gated by a `crossChecks` entry in the pack: four spellings count as applied (`id("app.cash.paparazzi")`, `id 'app.cash.paparazzi'`, `apply plugin: 'app.cash.paparazzi'`, and `alias(libs.plugins.paparazzi)`), and the id must be the **argument of an application** rather than merely present — a `testImplementation("app.cash.paparazzi:paparazzi-annotations:…")` names this plugin's artifacts and applies nothing, so a substring reading proposed the same broken row one indirection in. The alias is resolved in the catalogue **its own accessor names**, among the `gradle/*.versions.toml` files at the lane root (`libs.` is `gradle/libs.versions.toml`, a second catalogue is addressed by its own file stem, and a `testLibs.` accessor is never answered by `libs`), matched on the **whole key** Gradle generates — `libs.plugins.compose.paparazzi` is the `compose-paparazzi` entry or nothing — with the catalogue read as TOML (`#` to end of line, either line ending) and the build files read with the script stripper, so a commented-out application is not one in either language. Only **modules the lane's own settings file `include(…)`s** are read: a root `plugins` block naming the plugin `apply false` applies it nowhere, and a build file in a directory Gradle never configures is named as outside the build rather than counted. The answer is three-valued: `applied`, `absent`, and **`unknown`** for an `alias(…)` no catalogue of that name resolves, a settings file that builds its catalogues itself with `versionCatalogs {`, a dynamic `apply(…)`, a **root** build file that configures its modules from the top (`subprojects {`, `allprojects {` — the one signal the root is read for, never the id), or a `buildSrc/` or `build-logic/` whose convention plugins nen does not read. `absent` and `unknown` both withhold, as a **seat** that says which of the two it was, names every build file nen opened *and* every one it deliberately did not, and carries the honest alternative from the pack — for `test` the plain JVM row `{gw} :<module>:test` (never `testDebugUnitTest`), for `ui-test` that no screenshot task exists to propose and instrumented UI tests are unsupported on this stack. A plugin applied inside a **nested build** (a directory with its own wrapper or settings file) is named and not counted: it is applied to a build this lane's wrapper never runs. A row that also stops at an unanswered token now states **both** reasons (`… -- and independently: …`) instead of claiming the token is all it still needs. The plugin id lives only in the pack, pinned by a test that fails if `detect.ts` ever spells it ([#134](https://github.com/zheref/nen/pull/134), closes #128)
- **shu** — `nen shu detect` sorts every directory listing it walks, so one tree proposes one document. `readdirSync` returns the host's order and the host is not one host — this repository's suite runs under Node and the shipped binary is Bun, and the two answer the same unchanged directory differently — which changed the lane order, the marker list of a lane that matched twice, and (the correctness half) which of two lanes competing for one name got the bare one. The order is BYTE order everywhere, never `localeCompare`, so it does not depend on the machine's locale either: a repository with `apps/Beta` and `apps/alpha` now always proposes them in that order, in the lane list, in `--lane`'s refusal and in the key order of the written file ([#119](https://github.com/zheref/nen/pull/119), closes #116)
- **board** — `nen board render` and `nen board diff` validate the board file at the read seam and refuse a malformed board at exit 2 naming the file, row and field, instead of crashing with a raw `TypeError` (render) or silently diffing the string `"undefined"` (diff). Undeclared top-level board fields are now dropped from their `--json` output, as `build` already did for rows ([#99](https://github.com/zheref/nen/pull/99), closes #92)
- **epic** — `nen epic next-wave` treats every ref spelling inside a `blocked by` / `blocks` clause (bare `#N`, `owner/repo#N`, a markdown link) as an edge, never the line's identity, and finds the clause even when a prose `blocked by` precedes it; a line whose only ref sits inside a clause is reported as unparsed instead of becoming a phantom child ([#103](https://github.com/zheref/nen/pull/103), closes #97)
- **parse** — izanami's automation-policy classifier answers a verb that is mutating in every form (`tag cut`, `label apply`, `pr retarget`, …) as `mutating` with its own reason even when `--` is present, instead of a generic `unknown`; the refusal is unchanged ([#104](https://github.com/zheref/nen/pull/104))
- **parse** — the shell-metacharacter refusal renders the caller's line safely when it contains a quote or backslash ([#104](https://github.com/zheref/nen/pull/104))
- **backlog** — `nen backlog order --rows-from` validates the row document at the read seam and refuses a wrong shape at exit 2 naming the file, row and field — including `backlog fetch --json`'s own output, which must be reshaped first — instead of crashing with `{} is not iterable` ([#107](https://github.com/zheref/nen/pull/107), closes #105)

### Changed

- **schema** — the canonical location of `labels.json`, `repos.json`, `colors.yml` and `gates.json` is now the repository's `nen/` directory; `schemas/` is read as a fallback through the v0.3 line and `nen schema check` names every legacy read and shadowed leftover; the fallback is removed in v0.4.0. `nen/contract.json` (optional) is parsed and validated — `dependency` (what the repository needs from nen) and `project` (the stack declaration) — but nothing acts on it yet ([#109](https://github.com/zheref/nen/pull/109), closes #108)
- **schema** — a `project.toolchain` key is now validated at load, in both the contract loader and the profiles pack's: a tool name is held to npm's package-name shape (an optional `@scope/`, then letters, digits, `.`, `_`, `~`, `-`, not starting with `-`, `.` or `_`), because the key does not stay in the file — `shu tools --install` renders `<tool>@<version>` into the argv it spawns, so a key of `--all` would have become a flag to the installer. A declaration carrying such a key is now exit 1 naming the pointer; a `$`-prefixed key is still metadata and still skipped ([#120](https://github.com/zheref/nen/pull/120), closes #113)
- review debt: seven Copilot threads left open on #71, #72, #74, #75, #76 answered with pins for the `--gh-repo` shorthand path, relative `--body-file` cwd resolution and the `GIT_FETCH_PLAIN` line-scan hinge, plus explicit result types in the classifier CLI ([#104](https://github.com/zheref/nen/pull/104), closes #98)

### Added

- **shu** — `nen shu deploy` sends a build to a declared, **named** destination and to nothing else, and it takes **two flags with no single-flag path to acting**: `--target` says where (required, with no default ever) and `--run` says now. Without `--run` the verb prints the fully resolved plan — the destination substituted into the argv, every precondition asserted, each step as `would run:` — and spawns nothing, at exit 0; `--dry-run` is the explicit spelling of that same form and `--run --dry-run` together is exit 2 rather than a guess. It is the same dry-run-first gate `nen label apply --run` and `nen wake fire --run` carry, on the one verb in this family whose blast radius is other people's users, and it makes `shu deploy` the one executing row izanami classifies `write-flag-gated` rather than `dry-run-gated`: without `--run` the line is **read-only** because nen spawns nothing whatever the declaration says, a quoted `--run` it cannot prove absent is refused, and with `--run` in any spelling nen's own argv reader accepts it is mutating. `project.targets` is now parsed (`args` appended to the lane's own `deploy` argv — refused on a multi-step row, because which step reaches the destination is a guess, and refused, like any other argv, when they carry one of the reference pack's placeholder tokens; `requiresEnv` naming variables nen asserts are SET and whose values it never reads, compares, logs or prints, de-duplicated and held to a shell identifier at load, and asserted once when the lane's own preconditions already name one; `unsupported` for the destination that has no command line at all — a hosting provider's own push integration, a CI action — refused at exit 4 in the repository's own words, with the sentence required rather than just the key), and a key one edit from one of those four (`arg`, `requireEnv`) is refused by pointer naming the key it meant rather than preserved into silence. The report carries a `target` block (`{name, args, requiresEnv}`, `null` on every other verb) so a deploy that ran says where it went. The destination is resolved **after** the lane, the verb, the host and the placeholders and before the preconditions, which makes a written `deploy` seat reachable for the first time: a lane whose declaration says it has no deploy answers exit 4 with that sentence whatever `--target` says, instead of the old "no targets declared" that sent a maintainer to write a block which could not have helped. `nen shu detect` proposes `"targets": {}` on every stack and a destination on none, with a per-lane note carrying the reference pack's own word on `deploy`, the shape to write, and the rule that a credential value never goes in a committed file ([#135](https://github.com/zheref/nen/pull/135), closes #125)
- **cli** — a value flag given twice is now a usage error naming the value already read, family-wide, instead of silently taking the last one: `nen shu deploy --target preview --target production` used to send the build to production because of argument ORDER, with nothing on screen saying which of the two nen took. A flag that takes several values still takes them as one comma-separated value, and a repeated boolean is untouched ([#135](https://github.com/zheref/nen/pull/135), closes #125)
- **scaffold** — `nen scaffold init` is idempotent within a release but not across one, and now says so: the declaration it writes is `shu detect`'s proposal, which gained `"targets": {}` in this release, so a tree scaffolded by **v0.2.0** and re-run here reports the declaration `refused` (*already exists with different content*) rather than `skipped`. That is the verb's existing rule — it never overwrites a declaration and has no `--force` — applied to a proposal that grew; the block it would have written is printed above the refusal, an absent `targets` and an empty one behave identically, and every other step still reports `skipped` ([#135](https://github.com/zheref/nen/pull/135), closes #125)
- **shu** — `nen shu warmup` gets a working copy ready to iterate: refuses a dirty tree unless `--discard`, fetches and fast-forwards the trunk, creates the caller's branch, verifies the declared build and optionally the tests, printing every command under `--dry-run`. Every check that needs no mutation runs **before** `--discard` destroys anything, so a mistyped `--branch` costs nothing; `--discard` is `git reset --hard` then `git clean -fd` (never `-x`, never a second `-f`) and the tree is then **read again** — a nested repository or a dirty submodule that survived is named at exit 2 rather than reported as clean. It also refuses a half-finished merge/rebase/cherry-pick naming that operation's own `--abort`, and a detached `HEAD` carrying commits no branch and no remote-tracking ref reaches. The lane is resolved from the declaration on the branch it cut, not the tree it started in; a delegated `2` is reported as `1` (3/4/5 still pass through); the unrelated top-level `nen warmup` (registry pin sweep) is unchanged and both help texts say so ([#129](https://github.com/zheref/nen/pull/129), closes #124)
- **scaffold** — `nen scaffold init` is stack-aware for an existing repository (`--stack <id>` or `--accept-detected`, and it refuses naming both rather than guess one; `nen/contract.json`'s project block written into absence; the four taxonomy files still under `schemas/` **copied** into `nen/` with the `git rm` line printed and a differing pair refused; the stack's CI workflow from a bundled `templates/<name>/` data pack; `.nen/` appended to `.gitignore`; and a closing `nen shu tools` **check** that names the `--install` command instead of running it, unless `--install-tools` says so) plus a `--dry-run` that writes nothing and spawns nothing, probes included — the three v0.2.0 writes and their output are unchanged, and a second run reports `skipped` per item; `nen scaffold new` scaffolds a fresh project for any supported stack (`--stack --name --dir`, an empty directory it refuses to merge into, `{{name}}` substituted into the template's files, and `nen/contract.json` proposed by `shu detect` off the marker it just wrote, so a scaffolded project and a declared one cannot disagree — every post-step printed and none run). Every path either verb writes stays inside the tree it was pointed at: `--hook-path` and `--canon-values-path` are refused at exit 2 when they resolve outside `--repo` (the containment rule `nen shu` already applied to a declared path, now shared), and the two writes that create a directory — `nen/contract.json` and `.github/workflows/nen-shu.yml` — are additionally checked against the **real** path, so a symlinked `nen/` or `.github/` is refused naming the link rather than followed. The generated workflow is pinned at the greater of this binary's version and a minimum `templates/index.json` declares (the first release carrying the `nen shu` verbs it runs), overridable with `--nen-ref vX.Y.Z` and refusing one below the minimum by name, with a note saying nen cannot verify offline that a release exists for a ref; it also declares `permissions: contents: read`. A filesystem failure mid-run is a `refused` row carrying the errno and the report still prints, `.gitignore` is appended to byte for byte (a CRLF file is no longer rewritten, and the action is `appended`/`would-append`), a symlinked legacy taxonomy file is refused rather than copied in under a taxonomy file's name, both verbs write the commit-msg hook `0755`, and `scaffold new` refuses the `init`-only flags it used to accept and ignore ([#131](https://github.com/zheref/nen/pull/131), closes #114)
- **shu** — `nen shu tools` checks the toolchain a project declares in `nen/contract.json` (exit 5 when anything is missing or the wrong version, naming the install command per tool; the pack's tested minimum shown as advisory) and installs only through `corepack` with `--install`, on `darwin` and `linux` — on `win32` the install is refused with the commands to run by hand, because `corepack` is a batch shim there and this binary's one subprocess seam never uses a shell. Every other declared installer is verify-only in this release. `--dry-run` spawns nothing at all, probes included, which is the one form izanami certifies read-only; `--json` carries every value the table prints (`summary` plus a 15-field row, including each row's `remedy`, what an unreadable probe printed, and what an install ran) ([#120](https://github.com/zheref/nen/pull/120), closes #113)
- **profiles** — a bundled, inert catalogue of reference commands per stack (`profiles/<stack>.json`, seven stacks) that nothing which spawns a process may read (pinned by a source-scan test), and `docs/STACK-MATRIX.md`, generated from it by `bun run matrix` and drift-checked in the suite ([#112](https://github.com/zheref/nen/pull/112), closes #111)
- **shu** — new family `nen shu` (`detect build test ui-test lint archive release dev run deploy coverage tools warmup`): `detect` proposes a `nen/contract.json` project block from filename markers, taking each row from the bundled profiles pack, substituting `{pm}`/`{packageManager}` from the lane's own `package.json` and **withholding** — with the reason — every row it cannot cross-check against that manifest; it never writes without `--write` and never overwrites a declaration. The executor runs a lane's declared verbs, with `--dry-run` printing every command first, preconditions asserted and never performed, and a placeholder refused only when it is one of the pack's own tokens. It extends this CLI's published 0/1/2 with three codes: 3 (unsupported host), 4 (unsupported verb for this lane) and 5 (the declared program could not be started); a present-but-malformed declaration stays 1. `shu tools` and `shu warmup` are declared, documented and refuse at 4 in this release; the other eleven verbs run. `--json` is refused on `shu dev`/`shu run` unless `--dry-run` is given, because those two hand the terminal to the child ([#115](https://github.com/zheref/nen/pull/115), closes #110)
- **shu** — `detect` proposes the full `nextjs` row set (declared-only rows as explicit `unsupported` seats that say what they are, `coverage` when the manifest declares the task, one lane per workspace member) and the `gatsby` stack end to end (`gatsby build/develop/serve`, the archive and gh-pages steps, seats with their reasons). Two cross-checks are stricter with it: a pack token is answered from a declared script only where something CORROBORATES the match beyond word count, and a row is withheld unless the manifest declares the tool its package manager hands the work to (`<pm> <tool>`, `<pm> exec <tool>`, `npx <tool>`) and the runner's own config declares the task a `run <task>` names (`turbo.json`, not `package.json`'s scripts, for `turbo run x`) ([#119](https://github.com/zheref/nen/pull/119), closes #116)
- **shu** — the `gradle-android` and `compose-desktop` stacks: `detect` proposes `{gw} assembleDebug`, `{gw} verifyPaparazziDebug <unit-test task>`, `{gw} verifyPaparazziDebug` and `{gw} :app:lintDebug` for the first and `{gw} run` for the second, with the pack's `why` — the task must be `verifyPaparazziDebug`, never `testDebugUnitTest`, under which a snapshot test can overwrite a golden and still report PASSED — carried verbatim into the declaration, and the canon-vs-CI disagreement about that task reported as a note rather than encoded in any row. `{gw}` is the one token nen resolves itself: `detect` writes `./gradlew` on POSIX and `gradlew.bat` on win32, and only when that file is in the lane — a lane carrying the other host's spelling gets every row withheld naming both, because a missing wrapper is a finding and nen installs nothing; the executor still refuses an unsubstituted `{gw}` at exit 2. `{unitTestTask}` is answered only where the lane's own `settings.gradle{,.kts}` names exactly one module `detect` can SEE is a library: each included module is classified `application`/`library`/`unknown`, and one `unknown` — a missing directory, a `projectDir` remapped out of the tree, an `alias(...)`/`apply(...)` whose id lives in a version catalogue, or a convention-plugin id ending in the lane plugin's own last word — withholds the row naming the module and why, so an application module applying AGP indirectly is never proposed as `:app:test`. A lane must carry its own settings file before either stack matches, both comment forms are stripped before any build script is read (a `// TODO: id("com.android.application")` makes no lane, a `/* include(":retired") */` names no module), a marker pattern's `*/` prefix is honoured so an Android root's `apply false` is not a marker, and a directory named like the wrapper or the settings file is not one. A repository with two Gradle builds in one tree proposes exactly two lanes with `defaultLane: null`; one whose nested build has a settings file and no wrapper proposes one lane plus a finding naming the build nen could see and could not address; and a `compose.desktop` block one directory down that the lane's settings file includes is proposed as `{gw} :<module>:run`. Every lane whose rows carry a resolved `{gw}` also carries a note naming the host it was resolved for, and `hosts` is deliberately not narrowed for it ([#127](https://github.com/zheref/nen/pull/127), closes #117)
- **shu** — the `expo` stack: `detect` proposes the Metro lane end to end (`expo start`, `expo lint`, each only where the lane's own `package.json` declares `expo`), **never** a `build` row — `expo run:*` builds *and launches*, so the pack carries no such row and a seat quotes that reason at exit 4 — and withholds `run` naming `{platform}` plus every value the lane's own scripts spell in that position (`ios`, `android`) and where to state one; a bare-workflow tree (`ios/` and `android/` prebuild output committed, as in `zheref/food-diary`) becomes three lanes with `defaultLane: null` and no `hosts` block, and the Apple lane's shared scheme is read and cross-checked against the project's own targets, so a scheme naming a test target the `project.pbxproj` does not contain is reported as a clean-checkout failure rather than a missing placeholder ([#126](https://github.com/zheref/nen/pull/126), closes #118)
- **shu** — the `dotnet-winui` stack: `detect` reads a `*.csproj` carrying `<UseWinUI>true</UseWinUI>` (with any `*.sln` or `*.slnx` beside it as a second marker on the same lane), proposes `hosts: {"*": ["win32"]}` — exit 3 everywhere else, with the pack's host note carried as a lane note so the refusal says *why* — and, for the first stack whose ecosystem has no `package.json` to cross-check against, reads the project files themselves: `build` (`dotnet build <the solution, or the single WinUI project when there is none> -c Debug`) and `test` (`dotnet test <the project that carries the tests>`, proposed only where one exists), a `project.toolchain.dotnet-sdk` entry pinned from `global.json`'s own `sdk.version` (searched lane-upward and stopping AT the repository root, with a `rollForward` beside the pin reported as the floor a toolchain block has no field for), and a `path` precondition per `ProjectReference` that resolves inside the tree **and names a file that is there**. It withholds, with the reason, rather than guessing: a reference that **escapes the repository** to a sibling clone takes `build` and `test` with it, and so does one nen cannot resolve at all — an MSBuild `$(property)`, a `*`/`?` wildcard, a `;`-list, an `&entity;` — or one differing from disk only in case; each is quoted verbatim and asserted nowhere. Only the graph reachable from the file a row addresses counts, so a vendored project nothing points at is a finding rather than a withholding; a solution answers `{project}` only when it **lists** that project, so an unrelated, empty or stale one is named and stands aside; a tree resolving to two candidates names both and picks neither; a marker or a reference inside an XML comment is not one; a `<UseWinUI Condition="…">` is reported rather than silently missed; and a row a cross-check withholds is written as a seat carrying its reason instead of vanishing. MSIX packaging stays `unsupported`. Every rule above is data in `profiles/dotnet-winui.json` and rendered into `docs/STACK-MATRIX.md`, which now shows the answer ranks, the reference rules, and each toolchain entry's pin file and host-tool flag; `detect.ts` names none of it ([#132](https://github.com/zheref/nen/pull/132), closes #122)
- **shu** — the `xcode-ios` stack: `detect` answers `{project}` from the one `.xcodeproj` in the lane (lane-relative, and only where the container is the kind the reference row's own flag addresses — the flag is READ out of the pack's argv and decides which kind may answer: `-project` a lone `.xcodeproj`, `-workspace` a lone `.xcworkspace`, and a flag nen knows no kind for withholds naming the word it met; a workspace-only lane, two containers of the addressed kind, a workspace whose `contents.xcworkspacedata` nen could not read, and the CocoaPods shape where that file references the project are each withheld naming the flag, because a path of the wrong kind behind it fails on every machine), `{scheme}` from the one shared scheme whose cross-check against the project's own native targets was performed IN FULL and passed — every testable read against the container its own `ReferencedContainer` names, and a scheme that delegates its test action to a `<TestPlanReference>` followed into the `.xctestplan` and checked from there, so a target declared in a local Swift package or in a project one directory over is named as one nen did not check rather than reported as one the project has not got, and a plan-driven scheme (every project a recent Xcode creates) is no longer described as naming no target at all (two schemes, two projects, an unreadable `project.pbxproj`, an unreadable test plan, an out-of-lane container, a test action naming no target, and a test action naming a target the projects do not declare are seven different withholdings with seven different repairs), and `{resultBundle}` into nen's own generated `.nen/` — the one value `detect` contributes rather than reads, because it names an output; its note says the path is lane-relative, that `.nen/` is the line `nen scaffold init` appends to a repository's `.gitignore`, that `xcodebuild` refuses an existing `-resultBundlePath` so a second run needs the previous bundle gone, and that the bundle is a directory rather than the report a coverage reader parses. `{destination}` and `{simUdid}` are answered on **no** tree, ever: they name a simulator on the machine, `detect` reads a working tree, and the pack's cited forms are quoted once per lane as prose rather than lifted into a row. So this stack proposes no command row anywhere, and every withheld row now says what it DID answer and what is left — the same clause every other stack's withheld rows gained. `hosts` is the pack's `{"*": ["darwin"]}`, so a filled-in row refuses at exit 3 on linux and win32 with nothing spawned, and the seven cells the pack has no command for arrive as seats that refuse at exit 4 quoting its sentence. A `Podfile` is recorded as `evidence` beside the container, never as a marker; comments in a `project.pbxproj`, a `.xcscheme` and a `contents.xcworkspacedata` stop counting as facts; and a simulator destination survives as ONE argv element through the dry run's text, `--json` and the seam, pinned together on one row ([#133](https://github.com/zheref/nen/pull/133), closes #121)
- **docs** — `docs/USAGE.md`: every verb of all 34 families with purpose, arguments, exit codes, `--json` notes and a real example; conventions stated once (`--repo`, `--target`, exit codes, `--dry-run`, taxonomy as data, the bootstrap); six end-to-end developer workflows; a day-to-day-actions cross-reference ([#102](https://github.com/zheref/nen/pull/102), closes #90)
- **docs** — README gains a "Day to day" section: pin the binary once, set up a repository's taxonomy, and the four verbs a maintainer runs most, each runnable as printed. `docs/USAGE.md`'s workflow 6 is corrected to match: `backlog fetch`, `backlog order` and `board build` do not compose by file, and the reshape between them is the caller's ([#106](https://github.com/zheref/nen/pull/106))
- **shu** — `nen shu coverage` runs the lane's declared coverage verb and parses its report (Istanbul/Vitest, xccov, Cobertura/JaCoCo XML, LCOV) into one shape with totals and per-target rows, read from the first entry under the verb's own `artifacts` whose format nen recognises. `--threshold` only reports `met` and never changes the exit code, in either direction, and it is compared against the **counts** rather than the rounded percentage the table prints, so a project at 79.996% does not clear a bar of 80. A dry run parses nothing and a failed run is not parsed at all — a report on disk may be a previous run's — and a report that is truncated, or states more covered lines than it has lines, is a refusal naming the file rather than a number. Row names that resolve inside the repository are reported relative to its root, so a `--json` document does not carry the path of whoever ran it ([#130](https://github.com/zheref/nen/pull/130), closes #123)

### Breaking / consumer notes

nen is still on a 0.x line, so a minor bump is the vehicle for a breaking change, not just features — every item under "Changed" above is one, and `nen/` replacing `schemas/` as the canonical taxonomy directory is the one every consumer meets first (`schemas/` is still read as a fallback for the whole v0.3 line, and that fallback is removed in v0.4.0). Two consumers pin nen by tag and neither moves on its own:

- hatsu's `nen.contract.json` declares `minimum "0.1"`, which resolves to `>=0.1.0 <0.2.0` — v0.3.0 falls outside that range by design. Its repin PR needs a contract bump to `"0.3"` plus an updated `pinned_ref`, reviewed against the behaviour changes above.
- akatsuki-ai's vendored `bootstrap/nen.sh` carries a `DEFAULT_REF` pinned to `v0.1.0` and needs the same explicit repin, plus `nen scaffold init --accept-detected` to move its four `schemas/` files into `nen/`.

## v0.2.0 — 2026-09-07

nen's second release, and the first minor bump on its independent 0.x line. While nen sits at 0.x, a minor is where a breaking change ships — semver only promises no breaking changes between minors once a project reaches 1.0, and every one of the behaviour changes below is exactly that kind of change, not a bug reclassified as a feature. This release closes every p1/p2 issue filed against v0.1.0, across the 22 PRs below, cut via the release PR [#88](https://github.com/zheref/nen/pull/88).

### Behaviour changes a caller must know

These are deliberate contract changes, not bugs — a caller pinned to v0.1.0 behaviour needs to account for every one of them before repinning.

- `nen loop slots` now requires `--local-cap`; it no longer defaults the local concurrency plane to 7. The way out: pass `--local-cap <n>` explicitly. ([#69](https://github.com/zheref/nen/pull/69))
- `nen label apply`, `nen ref parse`, and `nen color status` now exit 2, not 1, on a typo'd token — matching this CLI's own contract that exit 2 means "you typed it wrong". The message text is unchanged, only the code; a retry wrapper or script branching on exit 1 for a bad ref or a bad `--category` must switch to 2. ([#83](https://github.com/zheref/nen/pull/83))
- `nen warmup` now fails (exit 1) on a consumer with no recorded pin, and gains an `unpinned` finding `kind` carrying `"pinned": null` in `--json` (widened from `string` to `string | null`). This is fail-closed on purpose: a check that could not be performed must never render as one that came back clean. The way out: record the pin, or update a caller reading `finding.pinned` as always a string. ([#83](https://github.com/zheref/nen/pull/83))
- `nen changelog completeness` and `nen release preflight` now both default `--fragment-dir` to `changelog.d`, and both refuse an empty value, a value naming a file, or a value that cannot be read — instead of the two verbs silently disagreeing about where fragments live, or one of them resolving an empty flag to the repository root. Omit the flag to get the default; pass an existing directory otherwise. ([#83](https://github.com/zheref/nen/pull/83))
- `nen board render` and `nen stop` now escape `|` as `\|` and flatten embedded newlines to a single space in table cells, so a title carrying either no longer shifts every later column. A consumer doing its own naive `split("|")` over these tables must switch to splitting on `(?<!\\)\|`, or use the exported `parsePipeTable`. ([#83](https://github.com/zheref/nen/pull/83))
- A gates file, a `--reviewers` name, or `wake --author-pattern` can now be refused at load time for a pattern with a potentially exponential-backtracking shape (`(a+)+`, `(a|a)+`, a large finite repeat over an ambiguous body) or one that matches the empty string (`.*`, `a*`, `x?`). The refusal quotes the offending fragment and names a rewrite; `.+` still loads, and both shipped fixtures and every product-codes registry checked against this pass load unchanged. ([#86](https://github.com/zheref/nen/pull/86))
- A relative `--gates` now resolves against `--repo`, not the current working directory, and the resolved absolute path is printed in `--explain` and in `--json`'s `meta.identities.path`. A caller relying on the old cwd-relative behaviour should pass an absolute path instead, which is used as-is and is unchanged. `nen pr next-blocker --gates` shares the same resolver. ([#86](https://github.com/zheref/nen/pull/86))
- An error escaping the entry point (`import.meta.main`) now exits 1 with a single `nen: `-prefixed message, instead of an unhandled-rejection stack dump and whatever exit status the host runtime happened to choose. ([#86](https://github.com/zheref/nen/pull/86))
- `nen issue attach-sub` and `nen issue consolidate-close` now refuse — before any write — when a number in `--parent`/`--children` names a pull request rather than an issue, and `nen idea file` fails loudly (exit 1) when its read-back verification lands on a pull request instead of an issue. The way out: pass an issue number, or verify the object by hand when `nen idea file`'s refusal says the issue was filed anyway. ([#82](https://github.com/zheref/nen/pull/82), [#84](https://github.com/zheref/nen/pull/84))
- `nen issue consolidate-close` without `--severity-family <ns>:<family>` used to silently union every child's severity label onto the parent at exit 0 whenever the children carried more than one label of the same family. It now refuses at exit 1, before any write, naming the colliding family and its labels. The way out: pass `--severity-family <ns>:<family>` naming the family whose strongest label should win. ([#62](https://github.com/zheref/nen/pull/62))
- `nen issue chain-position` and `nen issue terminus` now refuse when `--issue` names a pull request, for the same reason. ([#71](https://github.com/zheref/nen/pull/71))
- Two of the refusals above are pinned to a machine-readable `--json` shape rather than left to text-matching: `nen issue attach-sub`/`nen issue consolidate-close`'s pull-request refusal is `{parent, children, pullRequests, refused: true, reason}`, and `nen issue chain-position`/`nen issue terminus`'s classifier refusal is `{issue, refused: true, reason}` — both in that key order. ([#82](https://github.com/zheref/nen/pull/82), [#84](https://github.com/zheref/nen/pull/84), [#71](https://github.com/zheref/nen/pull/71)) `nen pr ready --explain`'s identities line is the same kind of fact for a caller that only reads text output: it now prints the resolved absolute gates path rather than a bare relative one, matching what `--json`'s `meta.identities.path` reports. ([#86](https://github.com/zheref/nen/pull/86))
- `nen issue comment` is a new verb — a general comment plus a per-child close-comment channel for consolidation. ([#75](https://github.com/zheref/nen/pull/75))
- `nen repo scenario`, `nen canon resolve`, `nen parse futon`, `nen wc classify`, `nen stage triage`, `nen idea file`, `nen labels sync`, `nen scaffold init`, `nen issue file`, `nen issue consolidate-close`, `nen tag cut`, `nen pr next-blocker`, `nen pr cascade-main`, `nen release resolve-target`, and `nen release self-check` — 15 verbs in all — now require `--repo`, matching what each one's own usage line already promised. Omitting it used to fall through to a silent cwd default (reading the wrong repository, or surfacing a raw "no such file" from an unrelated directory); it now refuses at exit 2 naming the flag. ([#73](https://github.com/zheref/nen/pull/73))
- `nen pr fetch` never issues a write call — every `gh api` argv it builds now names its HTTP method explicitly, closing a path where an unnamed method could resolve to something other than a read. ([#59](https://github.com/zheref/nen/pull/59))
- `nen release preflight` now parses `RELEASE_HOLD` for truthiness instead of merely non-emptiness, so `RELEASE_HOLD=false` and `RELEASE_HOLD=0` no longer hold a release the variable's own value says should proceed. ([#63](https://github.com/zheref/nen/pull/63))
- `nen parse izanami` and `nen watch until`'s read-only certification is tightened at the classification seam: a command that used to certify as `[read-only]` — including some quoted or metacharacter-bearing forms — can now refuse instead. A watcher relying on the looser certification should re-verify its commands still classify as expected. ([#74](https://github.com/zheref/nen/pull/74), [#76](https://github.com/zheref/nen/pull/76))
- A malformed `$`-prefixed key inside `schemas/repos.json`'s `product_codes` (e.g. `$comment`) now loads silently, skipped as metadata, instead of being read as a real product code and refusing or mis-reporting downstream. No action needed unless a caller depended on the old refusal. ([#81](https://github.com/zheref/nen/pull/81))

### Fixed

- **issue** — refuse `nen issue attach-sub`/`nen issue consolidate-close` when a number names a pull request ([#82](https://github.com/zheref/nen/pull/82), closes #77)
- **issue** — report the caller's own number from the classifier guard so `NotAnIssueError`'s contract holds ([#84](https://github.com/zheref/nen/pull/84))
- **issue** — refuse `nen issue chain-position`/`nen issue terminus` when `--issue` names a pull request ([#71](https://github.com/zheref/nen/pull/71), closes #25)
- **issue** — refuse `nen issue consolidate-close` when omitting `--severity-family` would silently union a family's labels ([#62](https://github.com/zheref/nen/pull/62), closes #22)
- **schema** — skip `$`-prefixed metadata keys when loading `product_codes` ([#81](https://github.com/zheref/nen/pull/81), closes #17)
- **parse / watch** — guard every read-only certification at the classification seam ([#76](https://github.com/zheref/nen/pull/76), closes #70)
- **parse / watch** — admit plain file reads and nen's own verbs in izanami's allowlist ([#74](https://github.com/zheref/nen/pull/74), closes #31)
- **parse** — stop a lone leading slot from swallowing a template's `[ ... ]` clause ([#67](https://github.com/zheref/nen/pull/67), closes #30)
- **repo** — require `--repo` where a usage line promises it, and split the scenario refusal by cause ([#73](https://github.com/zheref/nen/pull/73), closes #28)
- **repo** — resolve tokens from everything the registry records, not just `consumers[]` ([#66](https://github.com/zheref/nen/pull/66), closes #27)
- **loop** — require `--local-cap` instead of defaulting the local plane to 7 ([#69](https://github.com/zheref/nen/pull/69), closes #52)
- **board** — refuse malformed `--rows-from` rows instead of crashing in render ([#68](https://github.com/zheref/nen/pull/68), closes #32)
- **epic** — widen checklist parsing beyond the literal `- [ ] #N` line and surface unreadable checkboxes ([#65](https://github.com/zheref/nen/pull/65), closes #51)
- **backlog** — accept bare issue numbers in `order`'s `--blocks`/`--affects-consumers` and refuse unmatched tokens ([#64](https://github.com/zheref/nen/pull/64), closes #24)
- **release** — parse `RELEASE_HOLD` for truthiness instead of non-emptiness ([#63](https://github.com/zheref/nen/pull/63), closes #23)
- **split** — terminate hunk bodies per the `@@` counts, not at the next header ([#61](https://github.com/zheref/nen/pull/61), closes #21)
- **pr** — `pr ready`'s no-`#` shorthand (`BC925` → code `BC`, number `925`) now splits at the longest trailing digit run instead of conceding just one digit, so multi-digit PR numbers resolve correctly ([#72](https://github.com/zheref/nen/pull/72), closes #26)
- **pr** — honor `--gates` on `next-blocker` instead of silently ignoring it ([#60](https://github.com/zheref/nen/pull/60), closes #20)
- **pr** — make every `gh api` argv in the PR fetch name its method explicitly ([#59](https://github.com/zheref/nen/pull/59), closes #19)
- **pr** — resolve `--gates` against the repo root, catch the entry point, and refuse catastrophic gates patterns at load ([#86](https://github.com/zheref/nen/pull/86), closes #8)
- **cli-wide** — close the deferred verbs/3-shared findings: pipe-safe tables, usage exit codes, unpinned consumers, a shared fragment dir ([#83](https://github.com/zheref/nen/pull/83), closes #10)

### Added

- **issue** — add a general `nen issue comment` verb and a per-child close-comment channel ([#75](https://github.com/zheref/nen/pull/75), closes #29)

### Consumer note

nen is still on a 0.x line, so a minor bump is the vehicle for a breaking change, not just features — every item under "Behaviour changes" above is exactly that kind of change. Two known consumers pin to nen and need an explicit repin PR, not an automatic pickup:

- hatsu's `nen.contract.json` declares `minimum "0.1"`, which resolves to `>=0.1.0 <0.2.0` — v0.2.0 falls outside that range by design. Its repin PR needs a contract bump to `"0.2"` plus an updated `pinned_ref`, reviewed against the behaviour changes above.
- akatsuki-ai's vendored `bootstrap/nen.sh` carries a `DEFAULT_REF` pinned to `v0.1.0` and needs the same explicit repin.

nen's own `bootstrap/nen.sh` is byte-unchanged since v0.1.0 — the bootstrap itself carries no breaking change; only the binary it fetches does.

## v0.1.0 — 2026-09-01

nen's first release. nen is a CLI that turns a repository's own schemas, labels, and PR/issue graph into deterministic verbs an agent or a human can call instead of re-deriving the same judgment calls by hand — pull-request readiness, changelog completeness, backlog ordering, and the rest of the verb surface README documents.

See the [release page](https://github.com/zheref/nen/releases/tag/v0.1.0) and [#15](https://github.com/zheref/nen/pull/15).
