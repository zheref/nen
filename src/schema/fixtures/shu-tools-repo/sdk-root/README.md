TEST FIXTURE. An SDK root that EXISTS, so the `path-exists` versionFrom member
has something present to find: the fixture's probe answers with this directory's
repo-relative path, and the check is whether that path is there.
