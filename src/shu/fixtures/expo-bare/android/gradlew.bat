@rem TEST FIXTURE -- the native lane's Windows wrapper, never executed. It ships
@rem beside the POSIX one because `expo prebuild` writes both, and a bare
@rem workflow commits what prebuild wrote.
@exit /b 0
