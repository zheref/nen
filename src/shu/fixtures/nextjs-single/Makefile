# TEST FIXTURE -- a Makefile beside a lane. `nen shu detect` reports it as a
# FINDING and never proposes it: whether this repository's verbs route through
# one is its own call to write into the declaration.
build:
	@echo placeholder
