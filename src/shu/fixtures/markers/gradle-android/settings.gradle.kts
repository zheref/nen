// TEST FIXTURE -- the lane's own settings file. It is a MARKER, not decoration:
// the profile names it with no `contains`, and a directory that does not carry
// one is not this lane's build root. It names no module, which is why the
// `{unitTestTask}` row is withheld against this tree.
rootProject.name = "placeholder"
