// TEST FIXTURE -- the APPLICATION module. The plugin id below is the marker,
// and it is also what removes this module from the candidates for the
// unit-test task: the row that token fills is the JVM one.
plugins {
    id("com.android.application")
}
