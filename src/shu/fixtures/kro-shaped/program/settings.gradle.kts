// TEST FIXTURE -- the SECOND build's own settings file. It has its own root
// project name and is included by nothing above it, which is what makes this
// directory a lane rather than a module.
rootProject.name = "placeholder-desktop"
