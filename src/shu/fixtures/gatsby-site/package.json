{
  "name": "@placeholder/site",
  "private": true,
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "serve": "gatsby serve",
    "resume:pdf": "node scripts/build-placeholder-pdf.mjs",
    "deploy": "npm run resume:pdf && gh-pages -d public -b gh-pages --dotfiles"
  },
  "dependencies": {
    "gatsby": "5.14.0",
    "react": "18.3.1"
  },
  "devDependencies": {
    "gh-pages": "6.3.0"
  }
}
